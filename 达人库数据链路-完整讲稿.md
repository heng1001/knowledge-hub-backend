# 爬虫数据 → 后端处理 → 前端呈现：完整链路讲稿

前置说明：以下所有"真实项目"事实均来自 mibo 仓库（Java 后端，`/Users/mixianyun/Desktop/BE/mibo`）+ new-oa 前端（`/Users/mixianyun/Desktop/FE/new-oa`）的代码/配置/文档勘察；标注"数据量未核实"处为推断，勿在面试中编数字。

---

## 一、链路总览（8 环节，每个环节一句话 + 输入→输出）

1. **爬虫→后端数据契约（push/pull）**：爬虫用「按平台分 5 个 Kafka topic 的 push」把公海/达人池整包 JSON 实时推给后端，用「Redis zset 队列 + DB 条件更新」让后端按需调度爬虫 pull 补采。
   输入 = 爬虫抓取的平台公海/达人池原始 JSON；输出 = 5 个 Kafka topic（push）+ 4 条平台 zset 采集队列 + `erp_talent_collect_task` 任务表（pull）。

2. **接收与落库**：Kafka 消费者把整包 JSON 解析成各平台池明细表的"先查后 upsert"，落库后把 `*_kols_os.has_detail` 置 1 作为"详情是否齐全"的桥。
   输入 = topic 消息（或爬虫直写）；输出 = 各平台 `*_pool_*` 明细表（抖音 15+ / 小红书 12 / 快手 7-9 / B站 7-9 / 腾讯 7-9）+ `has_detail=1`。

3. **清洗加工**：把原始串（`'1.2w'`、`'10.5万'`）在内存按需换算成数值、去噪、空值化，供排序与聚合使用。
   输入 = pool 表原始串字段；输出 = 数值化/统一口径/空值兜底后的数据。

4. **建模存储**：按"池层 vs 达人绑定层"两层垂直分表，同平台再拆基础/画像/作品/报价/趋势，隔离 1:1 与 1:N 不同基数。
   输入 = 清洗后数据；输出 = 池层约 57 张 DO + 达人绑定层 29 张 + `erp_talent` 主表（`progress`/报价/淘汰字段）。

5. **同步检索**：一条 Maxwell binlog 双路输出到 OpenSearch（列表/详情/向量索引）与 ClickHouse（BI 分析），供跨库检索与聚合。
   输入 = MySQL binlog；输出 = 每平台 3 个列表索引 + 47 个详情池标量索引 + 每平台 1 个向量索引 + ClickHouse `mibo_bi` 库。

6. **API 服务化**：把 ES 检索 + MySQL pool 回查封装成列表/详情/工作台/脱敏四类接口，由前端消费。
   输入 = OpenSearch 索引 + pool 表；输出 = `talentListPageEs` / `platform-data` / `cross-platform-overview` / 敏感字段申请等接口。

7. **前端呈现**：React 端 5 平台达人库、达人详情多模块、图表懒加载、联系方式弹窗，按 URL/platform 分发。
   输入 = 后端 API 响应；输出 = 达人库列表页/详情页/工作台/图表/联系方式展示。

8. **数据回流**：业务操作（进度流转/报价/淘汰）写审计账本，缺详情与更新需求反向驱动爬虫补采，形成闭环。
   输入 = 业务操作 + 检索/使用中暴露的缺口；输出 = `erp_talent_stage_log` 审计 + 补采任务重新入队 + push 重抓回调 `is_all_data=1`。

---

## 二、逐环节展开

### 环节① 爬虫→后端数据契约（push/pull）

**核心职责**：定义"数据以什么形态、什么协议、什么可靠性"从爬虫交付给后端。本项目是**两条正交链路并存**：push（爬虫→后端，实时入池）与 pull（后端→爬虫，按需补采），并有一份设计文档明文约定两者独立、不复用。

**关键设计点**：
- push：按**平台**分 5 个 topic，每个 topic 内是**整包多实体 JSON**，无 version/msg_id/collected_at 等元数据字段，kolId 提取点各平台不同（硬编码路径）。
- pull：`erp_talent_collect_task` 一 kol 一行（唯一索引 `uk_platform_kol(platform,kol_id)`）+ Redis zset 做就绪信令 + 爬虫 `ZPOPMAX` 领取后**直写 MySQL** CAS 三态回写；状态机 0 待采集→1 抓取中→2 成功/3 失败→4 终态；`TalentCollectScanJob` 每 ~1min 做 5min 卡死判定、1/5/30min×3 指数退避、孤儿 PENDING 兜底重推。
- push 幂等：落库前查 `source='hc'`（人工维护高优来源）存在则整单跳过；新行统一 `source='cj'`；抖音/小红书/快手额外用 Redis 分布式锁 + `DeadlockRetryHelper` 死锁重试。

**真实项目对应实现**：
- push 消费者：`mibo-module-erp/mibo-module-erp-biz/src/main/java/cn/iocoder/mibo/module/erp/mq/talentpool/consumer/` 下 `DouyinTalentPoolKafkaConsumer.java` / `XiaohongshuTalentPoolKafkaConsumer.java` / `WechatTalentKafkaPoolConsumer.java`（腾讯互选）/ `BilibiliHuahuoPoolKafkaConsumer.java` / `kuaishou/KuaishouPoolKafkaConsumer.java`。
- topic/groupId：`douyin-talent-pool`/`douyin-talent-pool-group`、`xiaohongshu-talent-pool`、`tencent-talent-pool`、`bilibili-talent-pool`、`kuaishou-talent-pool`。
- kolId 提取：抖音顶层 `kol_id` 兜底 `payload.basic_info.id`；小红书 `payload.basic_info.data.userId/name`；腾讯 `payload.item.appid`（作 kolId）；B站 `payload.basic_info.result.upper_mid`；快手 `payload.basic_info.data.userId/name`。
- pull：`service/talent/collect/TalentCollectTaskServiceImpl.java`、`job/talent/SyncMissingTalentDetailJob.java`、`job/talent/TalentCollectScanJob.java`、`dal/redis/talent/TalentCollectQueueRedisDAO.java`、`enums/talent/TalentCollectStatusEnum.java`。
- zset key：抖音 `page_list:xingtu:xingtu_list`、快手 `page_list:juxing:juxing_list`、B站 `page_list:huahuo:huahuo_list`、小红书 `page_list:pugongying:pugongying_list`。
- 另一条 push 变体（更新回写）：`erp_talent_update_task`，后端 `POST /erp/talent-update-task/trigger-update` → 爬虫 `/update` → 回调 `POST /erp/talent-update-task/callback {taskId,status}`，成功置 `erp_talent.is_all_data=1`；`service/talent/ErpTalentUpdateTaskServiceImpl.java`、`job/talent/ErpTalentUpdateTaskJob.java`。

**真实 vs 理想差异**：
- 理想按实体分 4 个 topic（basic/stats/works/price）、按 kol_id 做 partition key 保序、消息带 version/msg_id/collected_at/source_fingerprint/cursor/trace_id、每 topic 一个 DLQ、落库用 `collected_at` 条件更新防乱序覆盖——**真实项目这些都缺失**，靠 Redis 锁 + 唯一键 + `source` 保护兜底；JSON 解析失败直接跳过、业务异常仅记日志不触发 Kafka 重试（无 DLQ）。
- **pull 链路高度符合理想**："Redis zset 信令 + DB CAS 权威 + 状态机 + 指数退避 + 卡死判定"与理想几乎一一对应，是这条链路最成熟的部分。

---

### 环节② 接收与落库

**核心职责**：把 topic 消息/爬虫直写数据可靠落到各平台池明细表，并维护 `has_detail` 桥标志，供缺详情 Job 读、供前端判断"详情是否齐全"。

**关键设计点**：
- 消费者校验必填 kolId → 提交各平台专用线程池（`douyinTalentPoolExecutor` 等）异步处理，线程内 `TenantContextHolder.setTenantId(1L)` → 调各平台 ProcessService。
- 抖音 `DouyinTalentPoolProcessService.processTalentPoolData`：Redis 分布式锁 `talent:pool:lock:douyin:{kolId}`（等 3s/持 30s）+ `TransactionTemplate`，按 kolId 逐表 `select→updateById 或 insert(source="cj")`，最后 `talentDetailStatusService.markDetailPresent(1, kolId)`。
- 幂等/覆盖：`source='hc'` 存在则整单跳过；每条子表按业务唯一键（kolId/itemId/portraitType/taskCategory/exportId）先查后 upsert，报价类整批删旧插新。
- 错误策略：JSON 解析失败直接跳过（毒消息不阻塞）；线程池业务异常仅记日志不触发 Kafka 重试；仅线程池提交失败等非业务异常 `throw RuntimeException` 触发 Kafka 重试。

**真实项目对应实现**：
- 各平台 ProcessService：`service/talent/douyin/DouyinTalentPoolProcessService.java`、`XiaohongshuDandelionProcessService`、`KuaishouPoolKafkaService`（`@Transactional`）、`BilibiliHuahuoPoolProcessService`、`TencentPoolProcessService`。
- 落库表：`douyin_pool_base_info/lastest_video/audience_portrait/price_info` 等、`erp_talent_xiaohongshu_pool_talent_*`（12 张）、`erp_talent_kuaishou_pool_*`、`erp_talent_bilibili_pool_*`、`erp_talent_tencent_pool_*`。
- 桥：`TalentDetailStatusService.markDetailPresent(platform, kolId)` 置 `*_kols_os.has_detail=1`；`hasDetail` 判定反向查 pool 表 `existsByKolId`。

**真实 vs 理想差异**：
- 理想要求 `collected_at` 条件更新（"旧数据不许覆盖新数据"），真实是覆盖式 `select→updateById`，**存在迟到的旧包覆盖新数据的乱序风险**。
- 理想每 topic 一个 DLQ 分流可重试/不可重试，真实无 DLQ，业务异常静默吞掉。
- 分布式锁只覆盖抖音/小红书/快手 3/5 平台——**B站/腾讯无锁**，同 kol 并发 `select→updateById/insert` 存在"双插重复行"与"旧包覆盖"竞态，未在讲解中主动渲染。

---

### 环节③ 清洗加工

**核心职责**：把池表的原始串（`'1.2w'`/`'10.5万'`/`'12000'`）换算成数值、去噪、空值化，供内存排序与 SQL 聚合使用；解析规则散落多处、语义并不统一。

**关键设计点**：
- `erp_talent.fans_count` 为 `varchar(255)` 存原始串，计算时内存按需解析，**不参与落库**。
- 核心换算 `parseNumberWithW`（`ErpTalentServiceImpl.java:2277-2309`）：末尾 `w/W/万` 判定后 ×10000、去千分位逗号、正则 `[^0-9.\-]` 全部剔除、`new BigDecimal`，异常/空白返 null。同文件 `parseDecimal:2314`。仅用于内存排序（fansCount/connectedUsers/expectedCpm/price21To60）。
- **存在第二套矛盾解析**：表单 VO 路径 `Long.parseLong(fansCount.replaceAll("[^0-9]",""))`（`ErpTalentFormServiceImpl.java:1559-1561`）会连小数点一起剥掉——`'10.5万'` 会解析成 `105`，与 `parseNumberWithW` 的 `105000` 语义不一致。
- 微信渠道清洗 `NewFormatParseHelper.java:466-500/541+`：显式处理 `"10万+"`/`"5千~1万"`/`"万"`/`"千"` → ×10000/×1000；CPM/比率类 ÷100；解析失败返 null。
- 池表 SQL 聚合：对 varchar 列 `CAST(... AS DECIMAL)` 且带 `IS NOT NULL AND != ''` 守卫（`mapper/talent/douyin/DouyinPoolCommerceSpreadInfoMapper.xml` 的 `cpm_20_60`、`mapper/talent/xiaohongshu/ErpTalentXiaohongshuPoolTalentCoreDataSumMapper.xml` 的 `cpm/imp/engage`）。
- 落库清洗 `DouyinTalentPoolProcessService.java:278-301` 空值化；联系方式异步解析仅 `self_intro` 变化时触发（`shouldTriggerContactAnalysis:303-316`）；粉丝数导出 `blankToDefault(...,"未知")` 兜底（`ErpTalentServiceImpl.java:2823/2932`）。

**真实 vs 理想差异**：
- 理想是集中式"单位映射表"配置化解析 + 存 BIGINT + 保留 `fans_raw` 原文 + `noise_issues`/`quality_score` + 异常值（粉丝骤变/封号）隔离进 anomaly 表——真实**无质量分、无异常突变检测**，且解析分散三套、语义互相矛盾（最易被追问穿帮）。

---

### 环节④ 建模存储

**核心职责**：设计"池层 vs 达人绑定层"两层垂直分表 + `erp_talent` 主表，承载基础/业务/财务/推荐/淘汰全量字段，靠 `kol_id`/`talent_id` 关联，无行级同步。

**关键设计点**：
- 两层垂直分表：① 池层 `erp_talent_*_pool_*`（约 57 张 DO，按 `kol_id` 关联，爬虫直写原始数据）；② 达人绑定层 `erp_talent_*`（29 张，按 `talent_id` 关联，业务快照）。同平台再拆 基础信息/画像/作品/报价/趋势。
- 基数差异显式化：1:1 表用 `UNIQUE KEY uk_talent_id`（`erp_talent_douyin_audience_portrait`、`erp_talent_bilibili_important_data`）；1:N 表只有 `idx_talent_id`（报价按 `level` 多行 `erp_talent_douyin_offerprice`、粉丝趋势按 `data_date` 追加 `erp_talent_bilibili_fans_trend`）；图片 blob 独立成 `*_pic` 表。
- 主表 `erp_talent`（`sql/main.sql:3820-3909`）：`progress int`(0 媒介已选…5 已签合同)、`initial_price/external_price/rebate/traffic_boosts_fee/off_platform_payment`(decimal)、`talent_match_score decimal(10,4)`、`recommendation`、`media_recommend_reason json`、淘汰字段（`eliminate_reason/before_status/elimination_operator_id/elimination_time/rejected_time`）、`kol_id`+`platform`+四平台 id 列。**仅 PRIMARY KEY(id)，无二级索引，查询走 ES/OpenSearch**。
- 历史采集表 `douyin_kols/kuaishou_kols/xiaohongshu_kols/bilibili_kols` 以 `data_json`+`latest_data_json` 存原始快照；`*_kols_os` 带 `has_detail` 标志。
- 更新机制：覆盖式 upsert 为主（基础信息/1:1 详情按 kol_id select→updateById）、趋势表按日期追加、报价表整表删重插（`replacePriceList`）；`erp_talent_stage_log` 是 progress 流转审计账本（`from_progress→to_progress`/`operation_type` ADVANCE/RETURN/REJECT/ELIMINATE/REVIVE/CREATE）。
- 多平台账号合并：**无跨平台合并路径**——`(kol_id, project_id, platform)` 三元组唯一，不同平台就是不同 erp_talent 行；四列 `douyin/kuaishou/xhs/bilibili_kol_id` 是建行时按平台写入的指针，已存在行只刷 name/avatar/fansCount/location，不合并第二平台 id。

**真实 vs 理想差异**：
- 理想主张"主表窄、详情宽、聚合另算"三层、不按维度 1:1 垂直分表、平台差异用 JSONB 兜底、主档+事实分离 + 统一 `kol_uid` 做跨平台实体识别——真实用拆表而非 JSONB、无 `kol_uid`、无跨平台合并（工作台 `getCrossPlatformOverview` 仅按商务对接人聚合展示，非账号合并）。
- 数据量：`erp_talent` AUTO_INCREMENT=3261209595，由外部雪花式大 ID 直插导致，**不代表真实行数**；从子表自增值看真实量级很小（`douyin_kols`=216、`kuaishou_kols`=122、`xiaohongshu_kols`=95、`erp_talent_douyin_offerprice`=136 等），"几百到几万行"属推断。面试可声明"行数未核实，自增值是雪花式直插的副作用"。

---

### 环节⑤ 同步检索

**核心职责**：把 MySQL 的池/达人数据经 binlog 物化到 OpenSearch（检索）与 ClickHouse（分析），双路支撑读侧；向量经火山 Ark embedding 降维后建 KNN。

**关键设计点**：
- MySQL→ES 增量：Maxwell 监听 binlog → Kafka → `MaxwellTestConsumer` → `TalentHandlerChain` 责任链按"库名+表名"分发到各平台 `TalentSyncStrategy` → 重查 MySQL 全子表 → Bulk 写 OpenSearch 标量索引 → `waitForDocumentVisible` 轮询可见 → `VectorSyncProducer` 发 Kafka → `VectorSyncConsumer` 按中文平台名 switch 分发 `mysqlSyncVectorInsertOrUpdate/Delete`（先 deleteByQuery 删旧再 `executeReindexBatchWithAggregation`）。
- 全量重建 `MySqlSyncOpenSearchDtsJob`：`deleteAllIndex`（按类型删 47 详情 / 12 列表）→ `createIndexTemplatesWithAnalyzer`（ik_smart，`has_detail` 默认 0）→ 火山 DTS（`mysql-03b7bf7a567d` → `o-00gsne5gndbg`，MySQL2ES）→ 10s 轮询进度 → `deleteOldDataFromListIndex` → reindex。
- 索引体系：每平台 3 个列表索引（kols 个人信息 `mibo_douyin_kols_os`、data 商业数据 `mibo_erp_talent_douyin_data_os`、price 报价 `mibo_erp_talent_douyin_os_price`）+ 47 个详情池标量索引（`mibo_erp_talent_douyin_pool_base_info` 等）+ 每平台 1 个向量索引（`douyin_pool_vector` 等，字段 `talent_industry_vector/content_keywords_vector/content_type_vector`；列表向量 `douyin_list_vector` 等）。
- 向量：火山 Ark `https://ark.cn-beijing.volces.com/api/v3/embeddings/multimodal`（`VolcanoEmbeddingApiServiceImpl`），BGE 1024/多模态 2048 维经 `VectorDimensionReducer` 统一降维到 128 维，`ReindexServiceImpl` 建 `knn_vector(knn=true)`。
- 检索实现：star 模块 `AbstractEsTalentListStrategy.talentListPage` 模板——推荐置顶 `prependProjectRecommendation`、search_after 深分页（无筛选用 `queryKolsPageKolIdsBySearchAfter(from,pageSize)` 跳过 `(pageNo-1)*pageSize` 每跳 1000 条，排序 `has_detail DESC, evaluation_scopes DESC, kol_id ASC`）、多条件 `queryKolIdsByConditions` 逐条件各查一个 kolId 集合再 `intersectKolIdSetsOrdered` 有序交集 + `sortKolIdsByEvaluationScopes` 重排、`queryAndAssembleVOs` 按 kol_id terms **并行查 kols/data/price 三索引** + `sanitizeAuditFields` 剥审计字段。
- ClickHouse：同一条 Maxwell 再输出 `maxwell-buried-point` → `BusinessTableConsumer`/`BuriedPointEventConsumer` → `mibo_bi`，10 张 `ReplacingMergeTree(version)` + `erp_talent_stage_log` MergeTree，`v_*` FINAL 视图；对账 `sql/p5-clickhouse-reconciliation.sql` + `docs/业务仪表盘-ClickHouse对账与口径映射.md`（可变表比视图、追加表比原始 ODS）。
- 脱敏：列表 ES 三索引**不存**联系方式明文，明文只在 MySQL pool 表，读取时 `StarTalentOsPlaintextLoader`（objectType `STAR_TALENT_OS`）临时回查 + `ResourceOverviewRespVO` 注解 `@MobileDesensitize/@RegexDesensitize/@EmailDesensitize`（`disable="@sensitiveMask.isOff()"`）；**但**详情 ES 文档 `ErpTalentDocument` 存 `realName/tel/address/email/idCard/bankAccount` 等明文，仅 `ErpTalentRespVO` 序列化层脱敏。

**真实 vs 理想差异**：
- 理想：binlog→MQ 主链路 + DTS 全量重建 + 每日对账 + 蓝绿重建（新索引→reindex→校验→切 alias→删旧）——真实重建是 `deleteAllIndex` **先删旧的破坏式**流程，`TalentEsRebuildRunner` 整文件注释禁用；**无 ES↔MySQL 每日对账**（只有 CH↔MySQL 对账）；无 binlog 消费延迟告警。
- topic 硬编码 `maxwell-mibo-20260813` 与配置 `${maxwell.talent-list-topic}`（仅作 groupId）不一致，属代码-配置契约断裂。

---

### 环节⑥ API 服务化

**核心职责**：把 ES 检索 + MySQL pool 回查 + 脱敏包装成前端可消费的列表/详情/工作台接口。

**关键设计点**：
- 列表：`POST /star/resource-overview/talentListPageEs`，三轨 endpoint 并存（`/page` 旧 MySQL 策略、`/page/mysql`、`/talentListPage`、`/talentListPageEs` ES 策略），由 `talent.elasticsearch.enabled` 门控，**靠换 endpoint 切换而非自动回退**。
- 详情聚合：`GET /erp/talent/platform-data?kolId&platform` 返回 `TalentPlatformData`（baseInfo/priceInfos/audiencePortraits/works/workWechat* 等），四平台共用。
- 工作台：`GET /fluencer/workbench/cross-platform-overview`（totalFans/recent30DaysPlayOrImpression/platformAccountDataList）、`GET /erp/talent/external/business-owner`、`POST /fluencer/workbench/refresh`。
- 脱敏出口：`POST /system/sensitive/plaintext` 申请明文留痕；`?masked=true`（`/erp/stock/get?masked=true`）；AI 解析 `POST /erp/company/parseContactOnDetail`。

**真实 vs 理想差异**：
- 理想是"瘦列表 + 富详情 + 异步导出 + 字典"四分法 + 并行聚合 + 池参数对齐 + 服务端统一裁决脱敏——真实**无字典接口**（筛选维度硬编码前端）、**无异步导出**（万级导出会打爆 HTTP）、无 `view` 角色分化、**脱敏靠前端传 `masked` 而非角色裁决**、无限流/幂等键/URL 版本信封/traceId 贯通。

---

### 环节⑦ 前端呈现

**核心职责**：5 平台达人库、达人详情多模块、图表懒加载、联系方式展示，交互受控（防抖/缓存/懒加载/竞态收敛）。

**关键设计点**：
- 达人库列表：`PlatformTabs` 用 useReducer 管理 5 平台独立状态，统一调 `getTalentListPage`（`src/api/erp/poolform/getTalentListPage.ts` → `POST /star/resource-overview/talentListPageEs`）；`buildRequest` 组参（platformId 映射 douyin=1/kuaishou=2/bilibili=3/xiaohongshu=4/shipinhao=5）；表格体 `PlatformTableBody`（半受控列渲染 defaultRender）。
- **特殊合并场景** `cooperationStatus=[2,3]`：`Promise.all` 并发调两次（page=1,pageSize=100，各传 status=2/3）→ 按 `kolId ?? kols.kol_id` 去重 → 写 `mergeCache` → 前端按 `(page-1)*pageSize` 本地 slice。
- 防抖：`DouyinSearchForm.tsx`（218 行 `setTimeout(...,400)`）`version` 状态驱动 useEffect + `isResetting` ref 跳过防抖；`handleReset` 立即触发。
- 达人详情：`src/views/talentDetail/index.tsx` `switch(platform)` 分发 1→douyin/2→kuaishou/3→bilibili/4→xiaohongshu，四平台都用 `useQuery` 调同一 `getTalentPlatformData(kolId, platform)`（queryKey `['talentPlatformData', kolId, platform]`）；抖音读嵌套 `data?.talentPoolData`（baseInfo/marketingInfo/priceInfos/spreadInfos/contentAnalysis/audiencePortraits/homepageVideos/lastestVideos + workWechat*）。
- 图表：`src/components/BaseChart`（echarts 按需注册 + IntersectionObserver rootMargin 120px 进入视口才 `echarts.init` 一次 + ResizeObserver rAF 去抖 + rAF 增量 setOption，注册 Semi 主题）；画像图 `AudiencePortrait.tsx`、粉丝趋势 `XiaohongshuFansTrend.tsx`/`PoolVideoTrends.tsx`/`PoolFansDay.tsx` 用 echarts-for-react。
- 联系方式：`ContactDialog.tsx`（手机号/微信号/QQ/邮箱/企业微信 + 复制 + 客户画像）；`parseContactInfo.ts` 优先扁平字段 phone/wechat/qq/email、兼容旧 contactInfo JSON；`SensitiveField/SensitiveGroup` 默认 maskedValue、点开选原因调 `/system/sensitive/plaintext` 申请明文。
- 工作台：`src/views/talent/worktable/`，React Query staleTime（businessOwner Infinity、crossPlatform 5min），刷新后 invalidate `['workbench','crossPlatform']`。

**真实 vs 理想差异**：
- 理想要求筛选状态 URL 化、虚拟滚动、AbortController 竞态取消、服务端原生多值筛选——真实**筛选状态未 URL 化**、无虚拟滚动、[2,3] 合并是全量拉取+内存切片（**每状态 >100 条会静默丢数据**）、竞态靠 React Query 同 key 隐式收敛。
- 详情 TS 接口与实际结构不符（douyin 读嵌套 `data.talentPoolData`），是**类型契约断裂**，讲解时勿当正常。

---

### 环节⑧ 数据回流

**核心职责**：业务操作产生审计账本；使用中暴露的"缺详情/数据旧"反向驱动爬虫补采；push 更新链路回调刷新 `is_all_data`，形成闭环。

**关键设计点**：
- 审计：业务操作改 `progress`/报价/淘汰 → `erp_talent_stage_log` 记 `from_progress→to_progress`/`enter_time`/`operation_type(ADVANCE/RETURN/REJECT/ELIMINATE/REVIVE/CREATE)`/操作人/备注，支撑阶段耗时（口径A/B）与回退次数（口径C）统计。
- push 回流：`ErpTalentUpdateTaskJob` 扫 FAILED & `next_retry_at<=now` → 调爬虫重抓 → 回调成功置 `erp_talent.is_all_data=1`，失败按 1/5/30min 指数退避、上限 3。
- pull 回流：缺详情（`has_detail=0`）→ `SyncMissingTalentDetailJob` 汇总建任务 → zset → 爬虫补采 → 落 pool → `has_detail=1`，周而复始。
- 读侧反向同步：Maxwell→Kafka→OpenSearch 同步 `has_detail`/标签；`MySqlSyncOpenSearchDtsJob` 全量重建兜底。

**真实 vs 理想差异**：
- 理想有 `batch_done` 批次数量对账闭环（manifest 预计数 vs 入库数，差异自动转 pull）+ 数据日水位表（last_cursor/last_snapshot_date）归位晚到乱序数据——真实无批次对账（现有 CH 对账是 MySQL↔CH 口径，非爬虫源量对账）、无水位表；"补漏转 pull"靠 `has_detail=0` 逐 kol 驱动，信号源与理想不等价。

---

## 三、全栈开发者职责清单（按环节）

**环节① 契约**
- 后端：定义 push 消息 schema（平台/实体/事件/采集时间必填）与 pull 任务字段；设计幂等键与乱序收敛策略；维护对接文档。
- 前端：不直接触碰契约层，但需在团队内对"列表/详情需要哪些字段"提出诉求，反向驱动后端在契约里预留字段。
- 关键动作：给 push 消息补 version/msg_id/collected_at 至少三个元数据；pull 侧确认"一 kol 一行 + 唯一索引 + CAS"。

**环节② 接收落库**
- 后端：写消费者（解析→校验→幂等→异步落库）；设计"旧数据不许覆盖新数据"的 collected_at 守卫；建 DLQ 与失败追踪；锁只覆盖 3/5 平台的缺口要补上 B站/腾讯。
- 关键动作：`has_detail` 桥的读写两侧都要有实现（落库置 1、缺详情 Job 读 0）；错误策略分级（毒消息跳过 vs 可重试 vs 人工介入）。

**环节③ 清洗加工**
- 后端：收敛为单一解析工具类（统一 w/万/千、剥千分位、null 兜底）；给 fans_count 落库 numeric 列 + 保留原始串；补质量分与异常突变检测。
- 关键动作：**消除 `Long.parseLong(replaceAll("[^0-9]",""))` 剥小数点的错误语义**——这是最容易被面试官抓包的矛盾点。

**环节④ 建模存储**
- 后端：设计主表 + 池层/绑定层分表，1:1 vs 1:N 用唯一键 vs 普通索引显式表达；`erp_talent` 主表补 `(tenant_id,deleted)` 与 `(kol_id,project_id,platform)` 复合索引作 MySQL 兜底；评估 1:1 低基数画像表合并或只读视图减少 join。
- 前端：向后端要"主档 vs 事实"语义，避免在展示层自行合并跨平台账号。

**环节⑤ 同步检索**
- 后端：Maxwell 配置 + 责任链策略 + 全量 DTS + 向量二次同步；补 ES↔MySQL 每日对账与 binlog 延迟告警；重建改蓝绿（新索引→切 alias→删旧）。
- 前端：消费 kols/data/price 三索引语义，理解"列表不存明文、联系方式回查"的查询成本。

**环节⑥ API 服务化**
- 后端：统一信封 `{code,msg,data}` + URL 版本 + traceId；抽 `GET /v1/dict` 字典接口；异步导出走 202+jobId+预签名 URL；限流（列表/详情令牌桶、导出独立更严）；**脱敏裁决收敛到服务端**（前端不再传 masked）。
- 前端：封装统一 request wrapper（baseURL/超时/拦截器/错误解信封）；401 single-flight 刷新；GET 幂等重试。

**环节⑦ 前端呈现**
- 前端：筛选状态 URL 化（searchParams + pushState + 滚动保持）；[2,3] 合并下沉后端多值筛选（去掉前端全量拉取+内存切片）；详情改 Tab 化 + fragment 直达；虚拟滚动对接 search_after。
- 后端：提供多值 cooperationStatus 原生筛选 + 显式 cursor 参数，替代前端模拟。

**环节⑧ 数据回流**
- 后端：审计账本写入点全覆盖（progress/报价/淘汰/涉敏字段查看/导出筛选集）；补"缺详情/数据旧"的驱动信号（has_detail + 批次对账双通道）。
- 前端：展示"数据完整/已更新"状态（is_all_data/has_detail），驱动业务人员触发补采。

---

## 四、面试讲述建议

### 5 分钟讲法（主次取舍）

- **0-30s 定位**：一句话——"这是多平台自媒体达人营销系统的数据主线，我负责爬虫数据从入口到前端呈现的完整链路，以及跨库检索与数据回流闭环。"（不要从环境搭建讲起。）
- **30s-2min 两条主线（重点）**：① push——爬虫按平台推 5 个 Kafka topic，后端消费者解析、分布式锁 + 唯一键幂等落库到池表，`has_detail` 置 1；② pull——缺详情 Job 汇总 `has_detail=0` 的 kol 建任务入 Redis zset，爬虫 ZPOPMAX 领取、DB CAS 三态回写，扫描 Job 做卡死判定与指数退避。这是整条链路最有故事性、也最符合"Redis 信令 + DB 权威"工程范式的地方，讲透。
- **2min-3.5min 读侧物化与检索**：一条 Maxwell binlog 双路输出 OpenSearch（责任链按表分发、向量经火山 Ark 降 128 维建 KNN）与 ClickHouse（ReplacingMergeTree 对账）；检索用 search_after 深分页 + 多条件交集 + 三索引并行装配。这里体现架构视野。
- **3.5min-4.5min 前端呈现**：5 平台达人库 useReducer + 400ms 防抖 + React Query 缓存 + 图表 IntersectionObserver 懒加载 + 联系方式回查。
- **4.5min-5min 收尾**：一句亮点（如 pull 链路状态机 + 幂等闭环）+ 一句已知改进（如"脱敏裁决正从注解层下沉到服务端"）——主动暴露改进方向比被动被问穿帮好。

### 面试官最可能追问的 5 个问题及应对

1. **"`talentListPageEs` 返回的 phone/wechat 是明文吧？`masked` 参数前端能不能传 false？"**
   应对（诚实 + 已做部分 + 改进）：答"是明文返回、`masked` 目前确实由前端传参，这是我们已知的合规短板；已做到的是列表 ES 三索引刻意不存明文、联系方式靠 `StarTalentOsPlaintextLoader` 临时回查 + `ResourceOverviewRespVO` 注解脱敏，正在把字段可见性裁决下沉到服务端、前端只消费已脱敏数据"。**绝不能讲"脱敏已统一"**。

2. **"爬虫直写 MySQL，有接口鉴权吗？它改任务状态 + 写 pool + 置 has_detail 的事务谁保证？"**
   应对：承认这是 pull 链路的设计权衡——用 DB CAS（`WHERE status=0/1`）+ 唯一索引 + 单任务单 worker 保证"一任务只被处理一次"，省掉一层 HTTP 接口和序列化开销；同时承认无接口鉴权/操作留痕是已知风险，改进方向是爬虫侧最小化 DB 凭据 + 加操作审计。

3. **"合作状态选 2+3 时你们怎么查？每状态只拉 100 条，超过就丢吧？"**
   应对：诚实承认"每状态 `pageSize=100` 全量拉取 + 前端去重 + 本地切片，真实命中超过 100 条会静默丢数据，这是正确性 bug"；改进是把这个多值筛选下沉到 ES 后端（`terms`/`bool`），前端只发一页请求。**别把"前端并行拉全量"讲成设计亮点**。

4. **"`'10.5万'` 在表单路径会解析成 105，在 `parseNumberWithW` 是 105000，你们到底按哪套？"**
   应对：承认两套解析并存、语义不一致是历史包袱，`Long.parseLong(replaceAll("[^0-9]",""))` 剥小数点是有 bug 的用法；改进是统一为单一解析 helper 并落库 numeric 列、原文保留。这是材料里最容易穿帮的点，主动认比被戳穿好。

5. **"retry 满 3 次进终态 4，但 enqueue 会把终态行重置回 PENDING，那抓不到的达人不是无限循环？"**
   应对：诚实承认这是设计缺陷——`SyncMissingTalentDetailJob` 按 `has_detail=0` 周期性建任务，会把终态(2/4)行重置回 PENDING 重新入队，`has_detail` 永远为 0 就永远循环；理想语义是"终态不自动流转、需人工介入"。给出改进：终态排除出 enqueue 来源 + 对已注销/永久失败达人打人工标记。

### 绝不能讲的点（结合真实性）

1. **不能讲"幂等/并发是多层防守、各司其职都想到了"**——缺 `collected_at` 乱序守卫，迟到的旧包会覆盖新数据，且 B站/腾讯无锁。
2. **不能讲"脱敏统一收敛、规则只在 VO 层"**——详情 ES 文档 `ErpTalentDocument` 存 realName/tel/address 明文、列表出口明文返回、`masked` 前端传参。
3. **不能讲"终态 4 防死循环"**——enqueueMissing 会把终态行重置回 PENDING，与"终态防死循环"自相矛盾。
4. **不能讲"对账闭环/机制等价已做到"**——无 batch_done 批次对账，现有 CH 对账是 MySQL↔CH 口径，非爬虫源量对账。
5. **不能讲"数据量亿级/海量"**——自增值 32.6 亿是雪花式直插副作用，真实行数很小（`douyin_kols`=216、`kuaishou_kols`=122 等），数据量未核实。也别主动提"为什么这么小的数据建 57 张 DO + 47 个 ES 索引 + 双路 Maxwell"，除非有口径（如"系统为多租户多项目未来扩容设计，当前规模下部分重量级组件偏重，是已知优化点"）。
6. **不能把全量重建讲成可靠手段**——`MySqlSyncOpenSearchDtsJob` 是 `deleteAllIndex` 先删的破坏式重建，删成功而 DTS 失败会留空库，无蓝绿兜底；`TalentEsRebuildRunner` 整文件注释禁用佐证其不稳。
7. **不能把"search_after 深分页"讲成游标语义**——实际是 `from=(pageNo-1)*pageSize` 每跳 1000 条的顺序跳过，是 from 的模拟，深页 O(n) 重扫。
8. **不能讲"分布式锁全平台覆盖"**——只有抖音/小红书/快手 3/5 平台有锁。
9. **别主动提及 topic 硬编码**——`maxwell-mibo-20260813` 硬编码与 `${maxwell.talent-list-topic}`（仅作 groupId）不一致，被问到时如实说"这是代码-配置契约断裂，改进是统一走配置"。
10. **不能讲"详情页类型契约一致"**——TS 接口声明与抖音实际消费的嵌套 `data.talentPoolData` 不符，被问到时承认是类型契约断裂、需用 generate API schema 对齐。