# 自建「多平台达人库」Demo — 实施规划

## Context（为什么做）

用户是前端同学，简历里"达人库筛选"亮点引用的是一套 Java/OpenSearch 后端代码（不是自己写的），觉得抽象、面试怕被深挖。目标：**亲手从零搭建一个达人库 demo**（Java 后端 + React 前端），把"多平台异构筛选 + 20+ 维度组合筛选"做成自己能讲透的项目。用户自己写代码，我全程规划 + 分阶段指导 + review。

**已确认的选型**（用户拍板）：
- 后端：Spring Boot 3.4 + Java 21 + Maven + **MySQL（本机 9.6 已在 3306 跑）+ MyBatis-Plus**（对齐简历/工作栈）+ **Spring AI**（自然语言选号 / 一句话画像）
- 前端：React 19 + Vite + TypeScript + Semi Design（沿用工作栈，不引新库）
- 范围：全功能 —— 4 平台异构筛选 + 分页 + ECharts 画像图表 + 达人详情页 + AI（① 自然语言选号 ② 一句话画像）

**参考真实实现**（已探明，方案照此建模）：
- 后端 DTO：`ResourceTalentListPageReqDTO`（4 平台各自独立 Filter 类 + 公共筛选）
- 后端服务：策略模式按平台分发查询再合并（`EsPlatformTalentListStrategyFactory`）
- 分表：`douyin_kols_os` / `kuaishou_kols_os` / `bilibili_kols_os` / `xiaohongshu_kols_os`
- 前端真实筛选表单：分平台渲染，字段口径完全不同

## 目录布局

```
~/Desktop/talent-library/
├── backend/    # Spring Boot + Maven
└── frontend/   # Vite + React + TS
```

> 放在 Desktop 新目录，独立可移植；不放进当前的 knowledge-hub-backend（那是 NestJS 项目）。

## 一、后端（Spring Boot 3.4 + MySQL + MyBatis-Plus）

### 1. 数据模型：4 张平台表（对齐真实分表设计）

每表公共列：`id, kol_id(unique), name, avatar, gender, province, city, fans_count, quote_type, quote_single, quote_all, cooperation_status, has_contact`

平台特有列（体现异构口径）：
| 表 | 特有列 |
|---|---|
| `douyin_kols` | talent_type, content_theme, **connect_users, cpm, cpm_type, play_count, interact_rate, complete_rate, viral_rate(爆文率)** |
| `kuaishou_kols` | talent_type, content_theme, **fans_growth, fans_growth_rate, deep_connect_users, cpm_video, cpm_live, play_count, interact_rate, complete_rate** |
| `bilibili_kols` | content_type, **play_median_30d_natural, play_median, interact_rate_median（中位数口径）, fans_growth_rate, fans_growth** |
| `xiaohongshu_kols` | content_type, **read_median_daily, interact_median_daily（中位数口径）** |

### 2. 后端结构

```
backend/src/main/java/com/talent/
├── TalentApplication.java
├── config/  CorsConfig(允许 5173→8080)、MybatisPlusConfig
├── controller/TalentController.java        POST /api/talents/page
├── controller/AiController.java            POST /api/ai/recommend（自然语言选号）、POST /api/ai/profile/{id}（一句话画像）
├── dto/TalentListReqDTO.java               内含 DouyinFilter/KuaishouFilter/BilibiliFilter/XiaohongshuFilter 内部类（照抄真实 DTO 字段）
├── vo/TalentVO.java                        统一返回（含 platform 字段）
├── entity/ DouyinKol / KuaishouKol / BilibiliKol / XiaohongshuKol
├── mapper/  DouyinKolMapper 等 4 个（继承 BaseMapper）
├── service/
│   ├── TalentSearchService.java            遍历 platformList → 策略分发 → 合并 → 排序 → 分页
│   └── strategy/
│       ├── TalentSearchStrategy.java       接口：List<TalentVO> search(TalentListReqDTO req)
│       ├── DouyinSearchStrategy.java 等 4 个实现  用 LambdaQueryWrapper 把条件全部 AND
│       └── TalentSearchStrategyFactory.java   platform → strategy
├── ai/
│   ├── TalentAiService.java               自然语言选号 + 一句话画像（编排下面两个）
│   ├── AiFilterExtractor.java             NL → TalentListReqDTO（Spring AI 结构化输出，复用筛选引擎）
│   └── TalentAiProfiler.java              详情数据 → 自然语言画像
└── seed/DataSeeder.java                    CommandLineRunner，空表时造 4 平台合成数据
src/main/resources/
├── application.yml                          MySQL 连接（库名 talent_library）、日志
└── schema.sql                               CREATE TABLE IF NOT EXISTS ×4
```

### 3. 接口设计

`POST /api/talents/page`，请求体（照抄真实 DTO 结构）：
```json
{
  "pageNum": 1, "pageSize": 20, "platformList": [1,2,3,4],
  "talentName": "小牛", "cooperationStatus": "合作中", "hasContact": 1,
  "douyin":   { "fansMin": 10000, "viralRateMin": 1, "connectUsersMin": 1000, "cpmType": "21-60s", ... },
  "kuaishou": { "fansGrowthRateMin": 1, "deepConnectUsersMin": 100, ... },
  "bilibili": { "playMedian30dNaturalMin": 1000, ... },
  "xiaohongshu": { "readMedianDailyMin": 100, ... }
}
```
响应：`{ "list": [TalentVO...], "total": n }`

**查询逻辑**（核心亮点，要讲得清）：
- 策略模式：按 `platformList` 逐个取 strategy，每平台用 `LambdaQueryWrapper` 把**公共条件 + 该平台独有条件全部 AND**（`eq/like/between/ge/le`）
- 跨平台合并：各平台匹配结果按 fans 降序，合并后统一切片分页（demo 数据量几千条，合并后内存分页足够、好解释）
- 多条件组合 = AND 交集语义

### 4. 造数据

`DataSeeder`（CommandLineRunner，某表 count=0 才造）：
- 抖音 ~1000 条、快手 ~800、B站 ~600、小红书 ~600
- 中文姓名（姓+名随机）、性别、省份/城市从固定列表取
- 粉丝数按量级分布（1千~1000万），CPM/报价与粉丝量正相关，互动率/完播率/爆文率在合理区间
- 平台特有指标按口径随机；cooperation_status 随机；has_contact 随机
- 头像用占位图 URL（picsum/dicebear 均可）

### 4.5 数据来源（demo vs 真实）—— 面试必问「数据哪来的」

**demo 用合成数据**，理由：
- 4 平台无公开免费 API；真实爬取有合规风险（《数据安全法》《个人信息保护法》收紧）
- demo 目的是讲清筛选引擎 / 异构建模 / AI 能力，合成数据足够
- 简历口径：**数据为脚本生成的合成数据，不写行数/真实数据量**

**真实企业（米播）四种来源**（按合规程度排序）：
| 来源 | 是什么 | 对应简历 |
|---|---|---|
| ① 官方开放平台 API（最合规） | 巨量星图 / 磁力聚星 / B站花火 / 小红书蒲公英，官方达人数据接口 | 对接巨量星图、磁力聚星（批量下单/停投） |
| ② 第三方数据服务商 | 蝉妈妈 / 飞瓜数据 / 新榜 / 千瓜，聚合多平台数据，按订阅买 API | 补官方 API 覆盖不到的维度 |
| ③ 采集/爬虫（灰色，做补充） | 抓达人公开主页数据，合规风险高，只做增量 | 达人详情采集任务体系（打分入队 → 爬虫拉取回写） |
| ④ 达人自主入驻（最干净） | MCN / 达人报名签约时自己填 + 授权 | 初始库来源之一 |

**真实链路串讲（面试讲法）**：初始库靠官方 API + 三方 + 达人入驻；增量靠采集任务体系补全详情（缺详情自动入队、按优先级打分、失败退避重试）；数据落 4 张分表 `*_kols_os`，画像聚合进 ClickHouse 数仓。

### 5. 兼容性注意
- MySQL 9.6 用 Spring Boot 管理的 `mysql-connector-j`（9.x）即可；建库 `CREATE DATABASE talent_library` 手动执行一次
- 首次运行 `spring.sql.init.mode=always` 执行 schema.sql，之后改 `never`

## 二、前端（Vite + React + TS + Semi Design）

`npm create vite@latest frontend -- --template react-ts`，装 `semi-ui semi-icons axios echarts`（echarts 动态 import）。

```
frontend/src/
├── api/client.ts            axios 实例 baseURL=/api，vite proxy → 8080，统一错误 Toast
├── api/talent.ts            postPage(params)
├── api/ai.ts                recommend(text)（流式）、profile(id)
├── types/talent.ts          TalentVO + 4 平台 Filter + TalentListParams
├── config/platform.ts       ★核心：每平台筛选字段配置（key/label/type/options），驱动动态渲染
├── hooks/useTalentList.ts   筛选 state + 400ms 防抖(关键词) + 查询/重置 + 分页
├── components/
│   ├── PlatformTabs.tsx          全部/抖音/快手/B站/小红书
│   ├── FilterPanel.tsx           读 platform.ts 配置 + 当前 Tab 动态渲染输入框/数字范围/下拉
│   ├── TalentTable.tsx           列配置按平台切换（动态列）
│   ├── TalentDetailDrawer.tsx    详情：雷达图 + 平台字段明细 + 报价 + AI 画像块
│   ├── AiChatPanel.tsx          自然语言选号：聊天框 + 流式回复 + 命中结果表格联动
│   ├── AiProfileBlock.tsx       详情 Drawer 里的「AI 画像」文字块（②）
│   └── Charts/  FansDistChart / PlatformPieChart / TalentRadarChart（echarts 懒加载）
└── pages/TalentLibrary.tsx      组装页
```

**前端核心亮点（面试讲点）**：
1. **配置驱动的动态筛选表单**：`platform.ts` 用数据声明每平台字段（含"爆文率""深度连接用户""播放中位数"等异构字段），FilterPanel 遍历配置渲染 → 加平台只加一份配置 + 一个类型
2. **400ms 防抖**：关键词输入防抖自动查询；`isResetting` 跳过重置时的无效请求（呼应工作项目 STAR5）
3. **多条件组装**：只把非空字段拼进 params，后端 AND 语义
4. **图表懒加载**：echarts 动态 import + 进入视口才初始化（呼应工作项目性能优化）
5. **AI 聊天流式渲染**：AiChatPanel 用 `fetch` ReadableStream 逐字渲染，命中结果表格联动（呼应全栈 AI 能力）

## 三、AI 能力设计（① 必做 ② 必做 ③ 选做 ④ 不做）

> 核心认知：达人库是**结构化数据**（20+ 维度、4 平台），AI 的正确姿势是「自然语言 → 结构化筛选」+「数据 → 自然语言叙事」，**不做 RAG**（RAG 是给非结构化文档用的，结构化数据检索走筛选/SQL）。这条认知本身就是面试加分点。

### ① 自然语言选号（旗舰，必做）

用户一句话 → AI 理解意图 → 转成 `TalentListReqDTO` 筛选条件 → **复用现有 TalentSearchService**（筛选逻辑一行不改）：

> 「帮我找抖音美妆达人，粉丝 50w–300w、爆文率 >10%、报价 5w 以内、能联系」

- **实现**：Spring AI `ChatClient` 结构化输出（JSON schema）→ `AiFilterExtractor` 把 JSON 映射为 DTO → 调用筛选服务 → 返回结果 + AI 一句选号理由
- **前端**：`AiChatPanel` 聊天框 + 流式回复 + 命中结果表格联动（前端强项）
- **面试讲点**：AI 只做语义理解，落库查询仍是策略模式那套 —— 能力叠加、不重写；呼应简历「达人智能推荐」

### ② 一句话画像（必做）

达人详情数据 → LLM → 自然语言画像：

> 「抖音美妆腰部达人，近 30 天爆文率 12%，互动稳定，CPM 偏低，适合品牌种草预算 3–8w 的项目」

- **实现**：详情数据拼 prompt → `TalentAiProfiler` 生成一段文字 → 详情 Drawer 加「AI 画像」块
- **面试讲点**：结构化数据 → 自然语言叙事；与①正好一对（人话→筛选 / 数据→人话）

### ③ 达人智能比对（选做）

勾选 2–3 个达人 → AI 生成对比表 + 一句推荐结论。

### ④ 内容图片分析（不做）

mibo 简历有「图片分析」，但 demo 需要真实图片数据、成本高，砍掉。

### 技术选型

- 后端 Java → **Spring AI**（`ChatClient` + 结构化输出）接 **DeepSeek**（便宜）或 **Ollama 本地 Qwen**（免费、无需 key）
- pom 增：`spring-ai-starter-model-openai`（DeepSeek 走 OpenAI 兼容协议）或 `spring-ai-starter-model-ollama`
- application.yml 配 base-url / api-key（Ollama 则不配 key）
- 前端流式：`fetch` ReadableStream 逐字渲染；接口统一错误 Toast

## 四、实施顺序（用户按此写，我逐步指导 + review）

| 阶段 | 内容 | 验收 |
|---|---|---|
| P0 | 建目录；backend 脚手架（pom 手写或 start.spring.io）、frontend Vite 脚手架 | 两端能起 |
| P1 | schema.sql + application.yml + 4 entity + 4 mapper | 启动成功、4 表建成 |
| P2 | DataSeeder 造数 | 表里有数据 |
| P3 | DTO + VO + 策略模式 4 实现 + Factory + Service + Controller | curl 验筛选/分页/多平台 |
| P4 | 前端 api client + types + vite proxy | 前端能调到后端 |
| P5 | TalentLibrary 页 + Tab + FilterPanel + Table + 分页 + 防抖 | 页面上能筛出结果 |
| P6 | ECharts 三张图（懒加载） | 图表渲染 |
| P7 | TalentDetailDrawer 详情 | 详情可用 |
| P8 | 联调打磨 + 简历文案 + 面试口径 | 全链路 + 文案 |
| P9 | AI① 自然语言选号：Spring AI 结构化输出 → 映射 DTO → 复用筛选引擎 | 聊天输入命中筛选并出结果 |
| P10 | AI② 一句话画像：详情数据 → AI 画像块 | 详情 Drawer 出画像 |
| P11 | 联调打磨 + 面试口径（含 AI 部分） | 全链路 + 文案 |

每阶段：我讲解概念 → 给关键代码骨架 → 你写 → 我 review 纠偏。

## 五、简历措辞与诚实口径

- 简历写"**独立设计并搭建**多平台达人库 demo"（自建学习项目，不冒充生产项目）
- "20+ 维度"指**跨平台累计**维度族；单平台 10~17 维（不要写"单平台 20+"）
- 数据为脚本生成的合成数据，**不写行数/真实数据量**
- 可讲的点：异构平台统一筛选建模、策略模式、配置驱动动态表单、防抖、组合查询 AND 语义
- AI 亮点措辞：**「自然语言选号 + AI 一句话画像」**（对齐简历「达人智能推荐」；demo 版用 Spring AI 接 DeepSeek/Ollama，面试说明是自建 demo 实现，非生产）
- AI 讲点：结构化数据用「NL → 结构化筛选」而非 RAG；AI 只做语义理解、复用筛选引擎；数据 → 自然语言叙事
- **数据来源面试模板**：「demo 是脚本生成的合成数据（诚实，不冒充）。真实项目数据来自：官方达人平台 API（星图/聚星/花火/蒲公英）为主，第三方数据商 + 采集体系做补充，达人入驻做初始沉淀——采集那条就是简历里的任务体系，负责把缺详情达人补齐。采集要合规，优先走官方/授权渠道。」

## 六、验证（端到端）

1. 后端：`mvn spring-boot:run` → curl 验证：单平台组合筛选（如抖音 粉丝 1w~100w + 爆文率≥1%）、多平台全选、分页、空结果
2. 前端：`npm run dev` → 浏览器验证：切平台 Tab 筛选字段联动变化、关键词 400ms 防抖、组合筛选出结果、分页、图表渲染、详情 Drawer
3. 全链路：4 平台各筛一遍，确认 AND 语义与结果一致
4. AI①：聊天框输入「抖音美妆达人 粉丝 50w~300w 爆文率>10% 报价<5w」→ 命中筛选并出结果；输入无关话术（如「今天天气」）→ 优雅兜底不报错
5. AI②：详情 Drawer 出「AI 画像」，冷启动无数据时给占位文案




## 接口
- POST /star/resource-overview/talentListPageEs — 跨平台达人搜索（走 ES）
- POST /erp/talent/link-to-project / batch-link-to-project — 把搜到的达人关联进项目
- POST /erp/company/parseContactOnDetail — 达人详情页 AI 解析联系方式（异步）

先明确范围："达人库"不是单一模块，是横跨两个 module 的功能切片——星图 mibo-module-star（跨平台达人搜索/推荐/达人库数据）+ mibo-module-erp（关联项目、AI 联系方式解析）。FE 菜单里 达人库 → /poolform 实际调的是 /star/resource-overview/talentListPageEs 和 /erp/talent/*。
     
  结论：有亮点，而且工程味主要集中在"抽象设计"和"降级/幂等"上，不是堆 CRUD。
     
  一、跨平台达人检索（最出彩的部分）
     
  - 模板方法 + 策略 + 工厂三层抽象：AbstractEsTalentListStrategy（service/resouceOverview/strategy/es/AbstractEsTalentListStrategy.java:31-188）把「筛 kolId → 分页 → search_after 取页 → 3 索引组 VO → 置顶推荐」固化成模板；子类只实现 getXxxIndex / queryKolIdsByConditions。工厂用 Spring 注入 List<Strategy> +
    @PostConstruct 自动注册进 ConcurrentHashMap，新增平台零改派发逻辑。同套策略接口还有 OpenSearch、MySQL 兜底两套实现（/talentListPage、/talentListPageEs、/page/mysql）。
  - search_after 深分页（:353-463）：绕过 ES from 1w 上限，翻页只取 _source.includes("kol_id") 省带宽。
  - 多条件筛选 = 有序集合求交集（:581-594）：每个筛选维度单独查 kolId 集合，LinkedHashSet.removeIf 做保序交集，天然保留主排序。
  - 跨库分段查询 + 内存归一（:210-266）：对一页 kolId 并发打 kols/data/price 三个索引，groupingBy(kol_id) 组装成统一 VO，sanitizeAuditFields 剥掉内部字段。
  - 达人名称两级检索：先 term(name.keyword) 精准，未命中再 match 模糊，兼顾召回与性能。
  - 每类查询单独 try/catch 降级：单维度失败只回退该维度原集合，不拖垮整页；ES client @Autowired(required=false) 未配置时优雅返回空页。
  
  二、达人推荐 + 外部 AI 编排

  - 打分做成纯函数：ScoringUtil 8 维权重统一收口 [0,100]，粉丝/播放用 log10(v/100)*10 压量级、性价比反向打分、触达效率用 sigmoid；四个平台 RecommendStrategy 共享数学、只喂各自平台字段（DouyinRecommendStrategy.java:92-121 等）。
  - LLM 只负责把 Brief 转结构化筛选条件（BriefRecommendServiceImpl 577-624）：temperature 0.1 强制严格 JSON，解析失败静默降级；单平台异常 continue 不拖垮其它平台。
  - 外部 AI 推荐（:8888）防护到位：120s 超时、非 2xx/非 JSON 一律返 null（TalentRecommendationHttpClient）；长调用放事务外 + 短事务逐行更新避免长事务持锁；独立线程池隔离 + 队列满 DiscardPolicy；连续 3 次失败熔断 60s 降级"标签匹配"；同 Bean @Async 失效用注入自身 self 代理解决；@Idempotent 防重复圈选入库。

  三、ERP 侧：关联项目 + AI 联系方式

  - 关联幂等做得很细：单条链路对 kolId+projectId 查重；命中 AI_RECOMMEND 推荐占位行时不重复插入，而是原地把 progress 升级（ErpTalentServiceImpl.java:590-639），避免先删后插破坏推荐上下文。
  - 批量 = 单事务 + 逐项容错 + partial success（:641-674）：单项失败绝不回滚整批，success()/partialSuccess() 工厂返回 total/successCount/failCount/failDetails，前端可精确提示"9 成功 1 失败(kolId 重复)"。
  - AI 解析联系方式的三道短路闸门（SingleKolContactParseService.java:65-220）：不存在 → 已解析 → 已标记 AI 解析 依次短路，简介为空走 fast-path 不空转 LLM；AI 调用在事务外，落库收敛成独立小事务；统一后处理管线（ContactExtractHelper）强制合法 JSON、修正 {{}}、兜底截首尾 {}，产出跨平台一致的规范 schema。

  四、数据模型 / 敏感数据

  - erp_talent 是"项目×达人"快照行，同人跨平台用外键列 douyinKolId/kuaishouKolId/... 关联；公海大表按平台拆 kols_os，明细再拆 DataOs/OsPrice。
  - 联系方式不落明文敏感库：contactInfo JSON 读时摊平并经 Jackson SpEL 统一脱敏（ResourceOverviewRespVO.java:76-93）；真正要明文时由 StarTalentOsPlaintextLoader 按需回查各平台 pool 表——"按需解密 + 读时脱敏"这套比常见的一脱到底/一存到底更实用。

  顺带：如果打算对外讲，有几处短板也建议心里有数

  - 老 OpenSearch 路径与新版 ES 模板同构复制了约 1.5 万行（Douyin 单策略 2905 行），难维护；老路径还留着 .size(10000) 截断风险。
  - generateEvaluationScopes 是全表循环 + 每行 3 次查询 + 每行 UPDATE 的 N+1，而 RecommendStrategyController 全接口 @PermitAll，等于任何匿名请求都能触发全量重算。
  - 批量关联是大事务：单项 catch 后不会自动回滚该项先前已写入的子表，极端场景可能残留脏数据；AI 解析出的联系方式目前是明文落 pool 表，没做字段级加密。

  要不要我把某一块展开（比如 ES 检索的 query 拼装细节，或推荐打分体系），做一次更深入的讲解？



