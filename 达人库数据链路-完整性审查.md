# 技术评审：爬虫数据到前端呈现链路讲解材料

以下评审严格基于材料内给出的事实，标注每条依据。

---

## 1. 【遗漏清单】

### 高严重度

**H1. 爬虫直写 MySQL 的安全/审计缺口未被作为风险讨论**
材料明确写"爬虫消费/回写（无后端接口，直写 MySQL）"、CAS 三态回写 `UPDATE ... SET status=1 ... WHERE task_id=? AND status=0`。即爬虫持有生产库直连写权限，无接口鉴权、无校验层、无操作审计。材料在环节A"做得好的 #2"把这条链路赞为"几乎完美对齐理想设计"，却全程未讨论其安全与审计风险（DB 凭据泄露面、越权改状态、无留痕）。作为讲解材料这是最大盲区。

**H2. 终态(4)会被 enqueue 任务重置为 PENDING，与"终态防死循环"自我矛盾且未被指出**
事实："已是终态(2/4)→复用同一行重置为 PENDING、生成新 taskId、先 ZREM 旧 member"。而 `SyncMissingTalentDetailJob` 周期性按 `has_detail=0` 建任务。于是"永久失败/达人已注销"的 kol：任务进终态 4 → 下一个周期 enqueue 又把它重置回 PENDING 重新入队 → 无限循环，`has_detail` 永远为 0。这与环节A row 16 声称的"终态 4 防死循环，符合理想"直接矛盾（理想设计的 terminal 是"不自动流转、需人工介入"）。材料既陈述了该事实，又下了相反结论，未察觉。

**H3. 前端"合作状态 2+3"合并存在数据丢失硬伤**
事实：`Promise.all` 并发调两次，各传 `page=1, pageSize=100`。若某状态真实命中 >100 条，只取了前 100 条做去重和本地 slice，超出部分静默丢失。环节D row 16 只评"部分"（建议下沉后端），未指出这个正确性 bug。

**H4. 脱敏合规结论被材料内部事实推翻**
前端出口明确写"达人库列表接口 talentListPageEs 本身不传 masked，联系人字段是明文返回"，且 `ContactDialog` 直接展示 手机号/微信号/QQ/邮箱。而环节D"做得好的 #3"却下结论"与理想『脱敏单点、规则只在一处』方向一致"。同时 `masked` 是前端传参（环节D section 4"脱敏开关 masked=true 由前端传参而非角色裁决"）——即客户端可自行决定是否要明文。合规结论站不住，却作为"亮点"呈现。

### 中严重度

**M1. B站/腾讯无分布式锁 + 无 collected_at 守卫 → 并发丢更新/重复插入未分析**
事实：锁只覆盖"抖音/小红书/快手"；且 row 5 已承认无采集时间条件更新。对无锁的 B站/腾讯，同一 kol 的并发 `select→updateById 或 insert` 存在"双线程都查不到→双 insert 重复行"与"旧包覆盖新包"两类竞态。材料对锁大加褒奖，却没指出 2/5 平台其实没有这层保护。

**M2. 监控告警体系严重不完整**
仅有 `TalentSyncLogService`（每步耗时日志）、CH 对账 SQL、ES 全量重建轮询。Kafka consumer lag、线程池业务异常（"仅记日志不触发 Kafka 重试"，即丢数据无人知）、DLQ（不存在）、任务队列深度、`has_detail=0` 覆盖率异常、爬虫侧健康，均无告警。环节C row 20 只一笔带过"补 binlog 消费延迟指标"。

**M3. 数据版权/来源合法性完全未覆盖**
爬取抖音/小红书等平台公海数据涉及平台 ToS、个人信息（联系方式、身份信息）采集合法性、GDPR/PIPL 合规。整份材料零提及。用户提示词里点了"数据版权"，材料确实没有。

**M4. ES↔MySQL 每日对账缺失**
环节C row 17 自己承认"ES↔MySQL 每日对账未看到"，只有 CH↔MySQL 对账。材料却在环节C"做得好的 #1"宣称"同步链路完整""跨库一致性有工具、有脚本、有文档支撑"——对账闭环并不完整。

**M5. 数据备份/容灾/恢复未覆盖**
无任何备份策略、binlog 保留、ES/CH 快照、演练事实。属完整链路讲解的应有环节。

**M6. 数据新鲜度 SLA 与迟到乱序归位未覆盖**
gap B row"增量窗口"已承认无 watermark 水位表、无 last_cursor/last_snapshot_date。材料没有讨论"数据多旧算可用""晚到数据按数据日归位"这两个运营口径问题。

**M7. 详情页 TS 接口与消费字段不符被当事实陈述而非缺陷**
"API 文件的 TS 接口与实际消费字段不符（douyin 读的是嵌套的 data.talentPoolData）"——这本身是类型契约断裂，材料只陈述不评价，也不在讲解中给应答口径。

### 低严重度

**L1. 自动化测试覆盖未讨论**（消费者单测、ES 同步测试、端到端联调验收——gap A row 26 只提契约测试缺失）。
**L2. 性能容量规划未讨论**（ES 索引分片、Kafka 吞吐、`search_after` 每跳 1000 的深页开销、57 张 DO/宽表拆分对真实几百~万行数据的性价比）。
**L3. 回滚/灰度与 schema 演进配套未讨论**（gap A row 24 提了演进规则但无部署回滚口径）。
**L4. `*_kols`（douyin_kols 无唯一键）与 pool/erp_talent 之间的同步正确性**只在 gap B 一句带过，未评估遗留表导致的重复风险。

---

## 2. 【站不住脚的结论】

按可靠性排序：

**S1. "终态 4 防死循环，符合理想"（gap A row 16）—— 最站不住**
事实层面即矛盾：enqueueMissing 会把终态(2/4)行直接重置回 PENDING 重新入队。理想中 terminal 的意义就是"不自动流转、防死循环"，这里完全没实现。且 gap A"做得好的 #2"又说 Pull 链路"几乎完美对齐理想设计"，两头打架。

**S2. "脱敏单点、规则只在一处"方向一致（环节D 做得好的 #3）—— 站不住**
自证矛盾：`talentListPageEs` 明文返回联系方式、前端 `ContactDialog` 直接展示、`masked` 由前端传参控制。ES 索引不存明文只能说明"检索侧无明文"，不能说明"出口合规"。把它当亮点讲会被一击穿。

**S3. "对账闭环（缺漏转 pull 补采）已做到、机制等价"（gap A row 22）—— 不成立**
理想是"batch manifest 数量对账 → 差异转 pull"；实际是"`has_detail=0` 逐 kol 补抓"。两者信号源不同（批次计数 vs 详情缺失标志），机制不等价。gap A row 21 自己已承认无 batch_done 对账，row 22 又宣称等价，前后不一致。

**S4. "并发/幂等是多层防守，每层各司其职"（环节A 做得好的 #1）—— 夸大了**
锁防的是并发写，`source='hc'` 防的是覆盖，唯一键防的是重复——但**乱序覆盖**（旧包覆盖新数据）这一层不存在（无 collected_at 守卫），而理想设计恰恰认为乱序是双保险的必备项。缺了一层却被描述为"各司其职都考虑到了"。

**S5. "已做到（比理想更细）"（gap A row 11，Redis 锁）—— 夸大**
锁只覆盖 3/5 平台；且锁与"条件更新幂等"是叠加关系而非替代关系（row 5 的 collected_at 缺失仍在）。既然 row 11 承认"注意与条件更新幂等叠加而非二选一"，就该把无锁平台的竞态一并讲清。

**S6. "search_after 深翻页"（环节C row 15 标"已做到"）—— 名不副实**
实际实现是 `from=(pageNo-1)*pageSize` 每跳 1000 条的顺序跳过，是 from 语义的模拟，不是游标语义；深页时 O(n) 重扫，且与理想"search_after 交互翻页"（拿上一页末文档做游标）不是一回事。标"已做到"属过度评级。

**S7. "全量重建"作为成熟工程手段被肯定（环节C row 18 标"部分"）—— 安全口径站不住**
流程是 `deleteAllIndex` 先删→建模板→DTS 灌→reindex，破坏式原地重建。若删除成功而 DTS 失败/超时（60min 上限），ES 侧留空库，无蓝绿兜底。`TalentEsRebuildRunner` 整文件注释禁用也佐证重建路径不稳健。作为讲解不能把它讲成可靠手段。

**S8. "数据量级在几百~几万行" 的自洽性问题（口径风险，非硬伤）**
`erp_talent` AUTO_INCREMENT=32.6亿 而真实行数小，材料解释为雪花式直插，自洽。但"采集库较小"（douyin_kols=216、kuaishou=122）与"抖音 15+ 张 pool 表、B站/小红书各 7-12 张"并存，且 47 个详情 ES 索引 + 12 个列表索引同步这么小的数据量——材料没解释"这么小的数据为什么这么重的存储/同步体系"，讲"57 张 DO、47 索引"时极易被听成数据量大。

**S9. "source='hc' 高优保护是好的实践"（gap A row 12、环节B 做得好的 #2）—— 忽略负面**
`source='hc'` 整单跳过意味着：一旦某达人被人工维护过，爬虫数据**永远**不更新它，即使人工数据已过期。材料只讲好处不讲 stale 风险。

---

## 3. 【讲解风险】（面试官最容易追问穿帮的点）

1. **脱敏/合规**：自夸"脱敏统一在 VO 层"时，一句"那 `talentListPageEs` 返回的 phone/wechat 是明文吧？masked 参数前端能不能传 false？"即穿帮。材料无任何关于服务端强制角色裁决的应答口径（事实上就是前端传参）。

2. **爬虫直连 DB**：Pull 链路"爬虫直写 MySQL"——"爬虫有你们生产库账号？没有接口鉴权？它改 task 状态+写 pool+置 has_detail 的事务谁保证？"材料把这条讲成亮点，却没有安全设计说明。

3. **前端 [2,3] 全量拉取**："每状态只拉 pageSize=100，超 100 条丢数据吧？为什么不在后端一次多值查询？"

4. **fans_count 两套解析**："`'10.5万'` 在表单 VO 路径会解析成 105（`Long.parseLong(replaceAll("[^0-9]",""))` 剥掉小数点），和 `parseNumberWithW` 的 105000 矛盾，你们到底按哪套？"——材料自己写了这个矛盾但没给"统一"结论，最易被当场抓包。

5. **终态语义**："retry 满 3 次进终态 4，但 enqueue 任务会把终态行重置回 PENDING，那一个永远抓不到的达人是不是无限循环？"（材料无应答口径，且是真实设计缺陷）

6. **B站/腾讯无锁**：夸"分布式锁防并发"时被问"全平台都有吗？"——只有 3/5。

7. **同优先级顺序**："小红书 score 固定 4000，同分任务在 zset 里按什么顺序出？"（material 无 score 编码公式，理想里的 `priority*1e12+now_ms` 没落地）

8. **topic 配置**："maxwell 的消费 topic 是配置驱动吗？"——事实是 `maxwell-mibo-20260813` 硬编码，`${maxwell.talent-list-topic}` 只用于 groupId，与配置名不符。

9. **重建索引**："重建流程先删旧索引，删完 DTS 失败怎么办？"——破坏式无蓝绿，`TalentEsRebuildRunner` 被整体注释，只能答"不可靠"。

10. **数据量级**："你系统多少达人？"——若答"亿级"穿帮；若答"几百到几万"，紧接着"那为什么建 57 张 DO、47 个 ES 索引、双路 Maxwell 同步？"——材料没有为"重体系对轻数据"给出辩护口径。

11. **详情页类型契约**："你详情页 TS 类型和实际返回结构一致吗？"——材料明确写了不一致（嵌套 `data.talentPoolData`），无合理解释。

12. **乱序覆盖**："没有 collected_at 守卫，一个迟到的旧包会不会把新数据覆盖了？"——gap A row 5 已承认缺失，是硬短板，讲解中不宜主动提"幂等完美"。

---

**总体判断**：材料的"事实层"质量高、自查也算诚实（缺失项大多在差距表里标了），但"结论层"（做得好的 3 个点）有 3 处自证矛盾（S1 终态复用、S2 脱敏明文、S3 对账等价），且把"爬虫直写 DB""前端全量拉取""重存储轻数据"三类硬伤当亮点/未高亮呈现——这是讲解最危险的地方。建议讲解前先把 S1/S2/S3 三个矛盾统一掉，并为 H1-H3、风险点 1/4/5/10 准备应答口径。