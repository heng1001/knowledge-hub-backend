# 面试 STAR：多平台异构数据统一（第 6 个亮点）

> 定位：这是你达人库里**最能体现「抽象能力 + 行业认知」**的一个点。
> 大多数候选人只能讲自己的业务模块；而"同一个达人，4 个平台 4 套数据模型，怎么统一筛选、统一比较、统一报价"——能把这个问题讲清楚，面试官会认为你有架构思维，还懂行业。
> 数字和字段名都是真实代码里的，背熟。

---

## 〇、一句话版（背这句）

> 「我们的达人数据来自 4 个平台——抖音、快手、小红书、B站。每个平台的数据结构、指标口径、数值单位、商业模型**全都不一样**：粉丝数有 4 种字段名，报价有 3 种数据格式、快手一家单位还是"分"，同叫"互动"的指标 4 个平台算法都不同。我的核心工作是做**多平台异构数据的统一**——后端用四层架构归一化，前端用配置驱动渲染，最终让运营能像筛一个平台一样跨 4 个平台筛达人。」

---

## 一、难点到底是什么

**异构体现在 5 个层面**（每层都是真实的）：

| 层面 | 具体冲突 | 真实证据 |
|---|---|---|
| **表结构** | 每平台一套 raw 表 + 8~16 张专属池表，结构完全不同 | 抖音池表 15 张、小红书 16 张、B站 8 张、快手 9 张 |
| **字段名（同义不同名）** | 达人介绍：抖音/B站叫 `introduction`，快手叫 `talent_introduction`；达人类型：抖音 `kol_type`，快手/小红书 `type` | `sql/main.sql` 4 张表 |
| **字段名（同名不同义）** | "预期CPM"：抖音是 `expectedCpm`，快手是 `overdueCPM`——语义不同却都叫"预期CPM" | `ErpTalentServiceImpl.extractExpectedCpm` :2343 |
| **数据格式（同义不同型）** | 报价：抖音/小红书/B站存 String，快手存 BigDecimal；`image_phone_price` 抖音是 decimal、快手是 varchar；粉丝数 raw 层全 varchar（存 `1.2w`）、pool 层全 Long、统一层又回到 String | 各平台 pool DO |
| **数值单位** | **快手报价单位是"分"**，其余平台是"元"；粉丝数有 `1.2w`/`10.5万`/`12,000` 三种写法 | `ErpTalentKuaishouPoolPriceInfoDO` :59-60 |
| **指标口径** | "互动"抖音叫 `item_interact_rate`（作品互动率）、快手叫 `nature_interaction_rate`（自然流量互动率）、B站叫 `interactRate`（互动率中位数）、小红书叫 `inter_mid_num`（互动中位数-日常）——4 个算法 4 个口径 | 各平台 Statistics DO |
| **商业模型** | 抖音有带货 GPM/星图档位、小红书有图文/视频双报价+种草 GMV、B站有花火 plus+弹幕收藏"三连"、快手有信用分/风险等级 | `DouyinPoolConvertAbilityDO`、`ErpTalentXhsOfferPrice`、`huahuoPlusCurPrice` 等 |

**一句话难点**：物理上 4 套完全不同的数据，业务上要求统一搜索、统一排序、统一报价——这就是"异构多源数据"这个经典问题的真实样本。

---

## 二、后端怎么解决：四层归一化架构（STAR 完整版）

**背景**：运营要在 4 个平台间筛达人、比报价、排 CPM。如果各平台各写各的逻辑，代码会爆炸；如果强行塞进一张表，平台特色字段全丢。

**做法——分四层，逐层抽象**：

```
raw 层  4 张独立表（douyin_kols 等） + data_json 全量 JSON 兜底
  ↓  爬虫原始数据原样落库，不翻译、不裁剪
pool 层  每平台 8~16 张专属池表（erp_talent_<platform>_pool_*）
  ↓  平台原生字段完整保留，天然异构
统一层  erp_talent 单主表（约 90 列）+ platform 枚举 + 4 个平台外键
  ↓  公共字段上收，平台特色字段做成"挂件"
消费层  策略模式(PlatformDirectDataStrategy) + 按平台 build 方法 + 统一评分(ScoringUtil)
```

每层的两个关键设计（面试重点）：

**1. 统一层：公共字段上收 + 「transient 挂件」模式**
- `erp_talent` 主表只存跨平台通用字段，`platform` 枚举(抖音1/快手2/B站3/小红书4) + `douyinKolId/kuaishouKolId/xhsKolId/bilibiliKolId` 4 个外键指回各自 raw 表；
- 平台特色数据用 `@TableField(exist=false)` 的**瞬态嵌套对象**挂载，不入库——快手挂 `kuaishouOfferPrice/kuaishouSpread`，抖音挂 `douyinOfferPrice/douyinSpreadValue`，小红书挂 `xhsNoteList`，B站挂 `bilibiliKeyData/bilibiliOfferPrice`；
- 数值统一存 String：`fans_count varchar(255)`，4 个平台写入时统一 `toString()`（`ErpTalentDirectDataServiceImpl` 各平台 build 方法 :1153/:2988/:4587/:5814）。

**2. 消费层：策略模式 + 责任链 + 统一评分**
- `PlatformDirectDataStrategy` 接口（`supports(platform)`）→ 4 个实现（`DouyinDirectDataStrategy` 等），`PlatformDirectDataStrategyContext` 用责任链按平台路由；
- `getTalentPlatformMetrics` 按平台 switch 到 4 个 build 方法，把各平台指标映射进**同一个** `ErpTalentPlatformMetricsRespVO`——前端拿到的是统一结构；
- 评分排序用 `ScoringUtil` 把 4 平台字段喂进 8 个维度函数，`parseNumberWithW` 把 `1.2w`/`10.5万`/`12,000` 统一成 BigDecimal，快手"分"除以 100 转"元"。

**结果**：新增平台只需加"一张 raw 表 + 一套 pool 表 + 一个 Strategy 实现类 + 一个 build 方法"，运营跨 4 平台统一筛选、统一报价排序。

**反思/踩坑**（面试主动讲的加分项）：
- 踩过 MyBatis-Plus 的坑：`updateById` 默认 NOT_NULL 策略不会把字段置 NULL——这是采集任务体系里的坑，但同理适用于这种多字段 DO；
- 快手 `KuaishouKolsDO.introduction` 字段和表列 `talent_introduction` 对不上（没有 `@TableField` 映射），说明"同义不同名"的坑真的踩过、且靠人肉发现——理想是配置化字段映射表。

---

## 三、前端怎么解决：异构表格的配置驱动渲染

**难点在 UI 上更直观**：抖音列 20+、小红书 **42 列**、视频号只有 **5 列**；筛选条件各平台完全不同（抖音有 CPM 档位、快手有深度连接用户、小红书有图文/视频报价类型、B站有播放中位数）；同一个概念文案还不同（粉丝数=粉丝数，但"播放"抖音叫预期播放量、B站叫播放量中位数）。

**做法——四个手段**：

**1. 配置驱动列定义 + 共享表体**
- 每个平台一个 COLS 数组（key+label+width）传入共享的 `PlatformTableBody`；
- `defaultRender(key, row)` 一个约 350 行的 switch 统一注册**所有平台的列渲染器**——列可以不一样，渲染逻辑统一收敛。

**2. 字段收敛函数 `processTalentItem`**（types.ts）
一个函数把 4 平台异构后端字段折叠成统一对象：
```ts
fansCount  = dataOs.follower ?? fans_number ?? fans_num
nickName   = kols.nick_name ?? nickname ?? name
price1_20  = getPrice('price120') ?? getPrice('price_1_20') ?? getPriceByName('1-20秒视频')
```
小红书每个指标还做**双后缀 fallback**（`x_30`/`x30d` 都兜）。前端用一套渲染代码，接 4 套后端数据。

**3. 数据适配器复用图表**：小红书的 `poolFansProfiles`、快手的画像数据，各自写一个 `transformPortraits` 转成抖音的 `PortraitItem[]`，**复用同一个观众画像图表**——平台数据不同，UI 组件同一份。

**4. 平台嵌套参数**：筛选条件不是平铺的，而是 `params[platform] = filters`（`{ douyin: {...}, xiaohongshu: {...} }`），POST 到统一接口——前端把"异构筛选"按平台名传下去，后端按平台名解析，前后端约定一条通道。

**5. 按平台切换文案**：`TalentInfoCell` 里 `{1:'抖音号',2:'快手号',3:'B站号',4:'小红书号',5:'视频号'}`，复制账号 Toast 文案跟着变。

**结果**：5 个平台共用一套表体、一套搜索交互（`FilterTagSingle` + `RateFilterPopover`），新平台只加 COLS + 表单。

---

## 四、行业对标：蝉圈圈 / 蝉妈妈（面试讲出行业视野）

> 面试官问"你们和蝉圈圈这些平台比，差距/特点在哪？"用这段。

**蝉圈圈的架构本质（背景知识）**：
- 蝉妈妈是**数据底座**（达人历史 GMV、带货转化率、粉丝重合度），蝉圈圈是**达人营销管理工具**——"数据 + 业务管理"双产品；
- 覆盖抖音、小红书、快手等多平台**一站式筛选**（据公开资料 B站覆盖弱，这恰是你的差异点之一）；
- 核心功能：全网找达人（253 万+）、达人合作管理（样品/物流/开播提醒）、营销效果追踪（GMV/漏斗/排行榜/爆单提醒）、内容种草管理、员工绩效核算；
- **指标分层**：媒介 PR 看"种草指标"（播放/点赞/收藏/评论情感/搜索指数），商务 BD 看"转化指标"（点击率/加购/GMV/ROI/坑位费）；
- **达人分层**：头部(500万+) / 肩部(100-500万) / 中腰部(10-100万) / 小达人 / 尾部；
- **带货口碑分**：商品体验+物流服务+服务态度三维加权（近90天）；
- **跨平台效果对比**：如"抖音达人 GMV 高但 CPE 贵，小红书达人互动高但转化慢"→ 指导投放比例；数据每小时更新。

**对标：你能对上的（加分）**：
| 蝉圈圈能力 | 你的项目对应 |
|---|---|
| 多平台一站式筛选 | 你的 ES 多维检索（策略模式 5 平台，蝉圈圈主覆盖 3 平台）|
| 达人分层/口碑分 | 你的 `ScoringUtil` 8 维度统一评分 |
| 合作管理闭环（筛选→合作→追踪）| 你的采集任务体系 + 达人库 + CRM 合作管理 |
| 跨平台指标对比 | 你 `getTalentPlatformMetrics` 把 4 平台塞进同一 VO，就是对比的前提 |
| 数据每小时更新 | 你的 Maxwell binlog → Kafka 增量同步 + 火山 DTS 全量重建 |

**对话术（面试官问差距时）**：
> 「蝉圈圈的护城河是**数据规模**（253 万达人）+ 数据底座与业务管理的闭环。我们创业公司量级小得多（真实达人数在千级），但**架构思路是同一个方向**：多平台异构统一、指标可跨平台对比、从选号到合作到追踪的闭环。我们要走的路，是把评分模型从 8 维度扩展成蝉妈妈那种口碑分体系，把「跨平台效果对比」做成运营的决策工具——比如提示"这个达人在抖音 GMV 高但 CPE 贵、在小红书互动高转化慢"，指导投放预算分配。」

> ⚠️ 红线：**别吹数据规模**。蝉圈圈 253 万，你真实几千——被问"你们库多大"时如实说中小型，然后立刻转到"架构怎么支撑未来扩容"。

---

## 五、能讲 vs 不能讲（追问红线清单）

**✅ 能放心讲**（有真代码支撑）：
- 四层归一化架构（raw→pool→统一→消费）；
- transient 挂件模式（公共字段上收、平台字段挂载）；
- 策略模式 + 责任链按平台路由；
- `processTalentItem` 字段收敛 + 双后缀 fallback；
- `parseNumberWithW` 统一 `w/W/万`；快手"分→元"换算；
- 配置驱动表格（PlatformTableBody + COLS + defaultRender）；
- 数据适配器复用画像图表（XHS/快手复用抖音 AudiencePortrait）；
- 平台嵌套参数的前后端约定。

**🚫 不能主动讲的**（被追问先承认 + 说改进）：
- **没有指标字典/配置化映射表**——4 平台字段映射全是硬编码的 build/strategy 逐字段手写。被问"新增一个平台指标要几步"→ 如实说"改 build 方法 + 加 fallback"，改进方向是配置化字段映射 + 指标字典；
- **没有跨平台实体归一化**——`erp_talent` 的 4 个 kol_id 只是指针，抖音的达人和小红书的同一个真人**没有打通**。别吹"跨平台打通同一达人"；
- **`parseNumberWithW` 有精度 bug**：`'10.5万'` 用 `replaceAll("[^0-9]","")` 去非法字符会变成 `105`（丢了小数点）。被问"单位转换有没有坑"→ 主动说这个 + 改进方向（正则保小数点 `[0-9.]`）；
- **快手 `KuaishouKolsDO.introduction` 与表列 `talent_introduction` 不一致**（同义不同名踩坑实例，人肉发现）；
- **`erp_talent` DO 里还有 `/* 临时注释:DO类缺失,待补充 */`**——统一 DO 不完整、仍在演进。被问"统一层完整吗"→ 如实说"还在演进，平台字段靠挂件补齐，下一步补全 + 配置化"。

**前端专属红线**：
- **DouyinTable 是孤儿组件**——最早整段复制，不走共享 PlatformTableBody。这块能讲成**演进史**（"从复制到抽象"）主动说：先有抖音整表，再抽 PlatformTableBody 给其余 4 平台复用，抖音版还没迁完，是重构 backlog；
- 列显隐/排序（`useColumnCustomizer`）状态在内存、组件卸载即丢，**没有 localStorage 持久化**；
- 筛选条件**没序列化到 URL**（刷新就丢）；
- 达人库列表**没有任何图表**——BaseChart 只用于 dashboard 模块，达人详情页图表是各平台手写（B站中国地图 registerMap 全项目唯一，不复用）。

---

## 六、2 分钟讲法（背这段即可）

> 「我们达人库的数据是 4 个平台异构的。后端我做了四层归一化：**raw 层**每平台一张原始表 + JSON 兜底，**pool 层**每平台一套专属表完整保留平台特色字段，**统一层**用一张 `erp_talent` 主表上收公共字段、平台特色数据做成 transient 挂件，**消费层**用策略模式按平台路由、把 4 平台指标映射进同一个 VO。
> 这中间最麻烦的是**口径统一**：粉丝数 4 种字段名、报价 3 种格式、快手单位是"分"要除以 100、`1.2w`/`10.5万` 要统一解析。前端也做了对应的统一——每个平台一个 COLS 配置进共享表体，一个 `processTalentItem` 收敛 4 套字段，小红书/快手用数据适配器复用抖音的画像图表。
> 和蝉圈圈这类行业平台比，他们的护城河是数据规模，但**架构方向一致**——多平台统一、指标可跨平台对比，这正是我们评分和筛选系统在走的路。」

---

## 七、为什么这个点面试官会"哇"

1. **它有一个经典问题名**：异构多源数据统一——任何做过复杂系统的面试官都懂这是硬骨头；
2. **你有"四层抽象"**：不是"我用了一张大表"，而是分层 + 挂件 + 策略模式，有架构品味；
3. **你有真实的坑**：快手"分→元"、`10.5万` 丢小数点、`introduction` 字段对不上——真实踩坑是加分项，比完美故事可信；
4. **你有行业视野**：主动对标蝉圈圈，说明你不只写代码，还知道行业头部产品在做什么；
5. **和你的其它亮点能串起来**：这个统一层是 STAR 2(ES检索) 和 STAR 5(多维筛选器) 的地基——筛选器能跨平台筛，前提就是这里做了统一。
