# 工作经历

**[公司名，米播科技待确认] ｜ 全栈开发工程师 ｜ [入职年月 – 至今]**

## 新媒体达人营销一体化平台（Mibo 后端 + New-OA 前端）

**项目定位**：面向新媒体/KOL 达人营销业务的企业级一体化管理系统，后端单体架构模块化拆分，前端由原 Vue3 版全量迁移重写为 React，覆盖「客户线索 → 商机 → 合同 → 项目 → 达人报价下单 → 内容执行 → 数据监控 → 结算出账」全链路，集成 ERP / CRM / HRM / 财务 / AI / 工作流 / 报表七大业务域。

**技术栈**：Java 21 · Spring Boot 3.4 · MyBatis-Plus · MySQL · Redis/Redisson · RocketMQ · Flowable 7 · Spring AI · ClickHouse · Kafka · EasyExcel ｜ React 19 · TypeScript · Vite · Semi Design · zustand · TanStack Query · ECharts 6 · 企业微信 JSSDK

**核心职责与成果（全栈）**：

### 后端（Java）

1. 基于 RuoYi-Vue-Pro 脚手架二次开发，按业务域拆分为 13 个 Maven 模块（system/infra/erp/crm/hrm/finance/bpm/report/ai 等），集成 Spring Security 的 RBAC 权限与多租户体系，统一 Web/安全/数据权限/缓存/消息等基础能力。
2. **统一项目毛利计算引擎**：将原五套毛利计算模式合并为一套统一公式（含对公/对私、灵活用工 6.5% 手续费、开票税率、赔款等），支持下单自动触发 + 页面手动计算双入口，事务隔离保证准确性。
3. **达人详情采集任务体系**：定时汇总缺详情达人 → 按标签/分类打分（最高 4000 分）推入 Redis zset 优先级队列 → 爬虫端拉取回写，配套 5 分钟卡死判定、1/5/30 分钟指数退避重试、冷却期与等待加权，覆盖抖音/快手/B站/小红书四平台。
4. **ClickHouse 实时数仓**：MySQL binlog（Maxwell）→ Kafka → ClickHouse CDC，支撑业务概览大屏 9 项指标、10 段漏斗、收入趋势与埋点分析，提供 MySQL↔ClickHouse 行数对账。
5. 多平台广告投放 API 对接：巨量星图、磁力聚星（批量下单/停投）、磁力引擎、腾讯互选；对接云账户灵活用工支付、飞书/企业微信 OA 集成。
6. Flowable 7 工作流支撑合同审批、推流申请审批；Spring AI 集成对话/RAG 知识库/Agent 工作流，落地达人智能推荐、图片分析；埋点驱动 11 阶段达人漏斗与绩效/提点计算。

### 前端（React）

7. 将原 Vue3 后台**全量迁移重写为 React 19 + TypeScript** 单页应用（约 23 万行代码、700+ 组件），覆盖系统管理、CRM、ERP 选号项目、BPM 审批、财务、达人库、报表等 20+ 业务模块。
8. **后端菜单驱动的动态路由 + RBAC 按钮级权限**体系：登录校验 → 拉取权限 → 按菜单动态构建路由；登录时并行预加载全局字典，多租户请求头统一管理。
9. axios 请求封装：401 **单飞刷新 Token + 请求队列重放**（并发请求刷新后自动重发）、业务错误统一 Toast、Blob 下载识别、RSA 登录加密。
10. **登录首屏性能专项：FCP 从 36s 优化至约 3s（提速约 12 倍）**——semi.css 异步加载、Semi UI 动态按需引入、posthog 延迟初始化、登录页组件懒加载、图片压缩 87%、Vite 手动分包。
11. ECharts 业务执行概览大屏：14 个后端接口实时数据、10 阶段漏斗图/收入趋势/排名环图/风险预警，IntersectionObserver 懒加载图表实例。
12. 企业微信深度集成（点击头像拉起会话/查看资料）、敏感字段脱敏审计（查看需填原因并记审计日志）、版本热更新检测 + chunk 404 兜底、GitLab CI 多环境构建部署。

**成果**：端到端业务链路贯通并投入生产；前端首屏加载提速约 12 倍；后端画像聚合等接口查询效率提升约 60%。

---

## 若按项目拆两条（投递系统里分开填）

**项目一：Mibo 后端** — Spring Boot 3.4 + MyBatis-Plus 模块化单体，覆盖 ERP/CRM/HRM/财务/AI/工作流，核心解决毛利统一核算、达人采集任务调度、ClickHouse 实时数仓与多平台投放对接。

**项目二：New-OA 前端** — React 19 中后台，Vue3 全量迁移，后端驱动动态路由权限、axios Token 刷新队列、ECharts 业务大屏，登录首屏 12 倍提速。

---

## 投递前必看（诚实口径）

1. **「成果」里的数字**：FCP 36s→3s、提速 12 倍是压测预期/Lighthouse 预估，非线上实测，面试说清楚；681ms→274ms（约 60%）是接口压测实测，可保留。
2. **后端职责 1~6**：要按实际参与程度取舍——如果某些模块不是你主导的，至少先把这个模块的代码看透（读 Controller/Service），面试被追问要能接住。第 2/3/4 条（毛利引擎、采集任务体系、ClickHouse 数仓）是 mibo 最硬的后端亮点，优先准备。
3. **前端职责 7~12**：前端真实主场，可以全部自信保留；其中「全量迁移」是最有掌控感的一条，可作面试开场。
4. 第 8 条动态路由里 `collectRoutes` 对 `.vue` 后缀的兼容处理、第 9 条 Token 刷新队列的 `isRefreshToken` 互斥 + `requestQueue`，都是容易展开深挖的点，提前背熟。
