# 简历项目经历：多平台自媒体达人营销系统（达人库）
-----------------------------------达人库-----------------------------------
1. 多平台：抖音/快手/小红书/B站，数据处理
- 筛选条件
- 数据存储
- 


> 依据《达人库数据链路-完整讲稿》《达人库数据链路-完整性审查》《面试STAR故事-五个亮点》三份材料生成。
> 已通过真实性审查：每句数字均可溯源，未触碰"绝不能讲"红线。
> ⚠️ 头部三段占位符（时间段/角色/上线状态）请按真实情况填写后再投递。

---

## 一、推荐版（300~500 字）

**多平台自媒体达人营销系统（达人库）**
`[时间段：20XX.XX – 20XX.XX ｜ 角色：后端 / 全栈工程师 ｜ 上线状态：生产/联调（待填）]`

面向运营的达人资源管理与营销撮合平台，覆盖抖音/快手/B站/小红书/视频号 5 平台，负责爬虫接入、落库建模、跨库检索到前端呈现的完整数据链路。

**技术栈**：Java / Spring Boot / MyBatis-Plus / MySQL / Redis / Kafka / OpenSearch / ClickHouse / Maxwell(binlog) / React(TypeScript) / Vite / Semi UI

**核心职责与量化成果**：

1. **设计 pull 补采任务体系**：Redis zset 做就绪信令 + DB CAS 状态机做权威，(platform, kol_id) 唯一索引实现一 kol 一行天然去重、单任务仅被处理一次；配套 5 分钟卡死判定、1/5/30 分钟×3 指数退避、孤儿任务兜底重推，任务卡死自动兜底恢复。

2. **实现 OpenSearch 多维检索**：策略模式抽象 5 平台检索实现，新增平台仅加一个策略类；每平台并行装配 kols/data/price 3 索引，多条件先查 kolId 集合再有序交集合并，支撑 20+ 维度组合筛选；数据经 Maxwell binlog→Kafka→责任链增量同步至 OpenSearch 与 ClickHouse。

3. **优化画像并发聚合**：CompletableFuture 并行聚合每达人 17 组平台数据，有界队列 + CallerRunsPolicy 与 DB 连接池 max-active=20 匹配实现背压。压测实测：画像查询 681ms→274ms（-59%），17 组并发总耗时 1411ms→357ms。

4. **React 实现 5 平台达人库列表与详情**：400ms 防抖 + React Query 缓存 + IntersectionObserver 图表懒加载；将最贵依赖 semi-ui（6.4MB、占首屏 49%）经动态加载从首屏 import 链摘除。压测预期：FCP 36.4s→约3s（约12 倍）、首屏传输 13MB→约4MB、首屏 JS 8MB→约1.5MB。

**结果**：端到端链路贯通，5 平台可检索；后端画像查询提速 59%、17 组并发缩短约 75%，前端首屏 FCP 预计提升约 12 倍。

---

## 二、紧凑版（150~200 字，招聘系统/邮件投递用）

多平台自媒体达人营销系统（达人库）：Java/Spring/MySQL/Redis/Kafka/OpenSearch/React 全链路，覆盖 5 平台（抖音/快手/B站/小红书/视频号）。自建 pull 补采体系（Redis zset 信令 + DB CAS 唯一索引去重 + 5min 卡死判定 + 1/5/30min×3 退避）与 push 双链路；OpenSearch 策略化检索，支撑 20+ 维度组合筛选；CompletableFuture 并行聚合 17 组数据，画像查询 681ms→274ms（-59%）、17 并发 1411ms→357ms；React 首屏优化，预期 FCP 36.4s→约3s、JS 8MB→约1.5MB。

---

## 三、按岗位调整

### 面向后端 / 数据工程师岗
把职责 4（前端）压缩成半句或删除；把「画像并发优化」提到职责 1 之前，以「17 组并发 1411ms→357ms」为主指标领衔。正文可补回被裁剪的深度：线程池 core=10/max=15/queue=100 + CallerRunsPolicy 与 DB 连接池 max-active=20 的参数匹配、Maxwell 双路物化、47 个详情池标量索引 + 128 维向量 KNN（火山 Ark 降维）。
主动准备两问口径：「调的是阿里云 OpenSearch 托管索引而非自建 ES 集群」「pull 终态行会被 enqueue 重置回 PENDING、永久失败达人会循环补采」（STAR1 诚实口径），以及 search_after 实为 from 顺序跳过、深页 O(n) 的实现细节。

### 面向前端工程师岗
把职责 4 提到第一位并扩写，标题明确写「**独立实现** 5 平台达人库前端（React+TypeScript+Vite）」，前端指标为主（全部保留「预期」限定），后端 681ms→274ms 等降为背景一句带过。
可补回细节：semi.css 778KB 异步化 + preload 回退、posthog 埋点 296KB 空闲加载、400ms 防抖 + isResetting 跳过重置无效请求、ECharts 图表懒加载。
诚实口径：FCP/首屏/JS 三组数字是「压测预期 / Lighthouse 预估」非实测达成；图片仅三张大图 PNG→JPG 落地（登录背景图 1.3MB PNG 未做），不得写成全量完成。

---

## 四、简历数字 ↔ 面试对应（面试前背熟）

1. 「5 分钟卡死判定 + 1/5/30 分钟×3 指数退避 + 孤儿任务兜底重推」↔ STAR1 + 讲稿环节①/⑧。
   被追问"那永远抓不到的达人怎么办"：诚实承认 pull 终态行会被 enqueue 重置回 PENDING、has_detail=0 驱动循环补采，这是已知设计缺陷；改进方向是终态排除出 enqueue 来源 + 人工标记。**不要辩解"自愈"**。

2. 「(platform, kol_id) 唯一索引一 kol 一行天然去重、单任务仅被处理一次」↔ STAR1。
   口径：去重/一次仅限 pull 任务内（CAS WHERE status=0/1 + 唯一索引），不是全局幂等；分布式锁只覆盖抖音/小红书/快手 3/5 平台（B站/腾讯无锁），也没有 collected_at 乱序守卫。

3. 「push 链路按平台拆 5 个 Kafka topic + has_detail=0 驱动补采」↔ STAR1/讲稿。
   口径：push 的 5 个 topic 实为 抖音/小红书/腾讯互选/B站/快手，与前端"5 平台"映射集不完全一致；"补采闭环"仅指 has_detail=0 反向驱动，**不是 batch_done 批次对账（无对账闭环）**。

4. 「OpenSearch 策略模式 5 平台检索、3 索引并行装配、20+ 维度组合筛选」↔ STAR2。
   口径：实际是阿里云 OpenSearch 托管索引（Maxwell 物化 + kNN 向量索引），别讲成自建 ES 集群；20+ 维度组合筛选能力真实，但维度硬编码在前端、无服务端字典配置接口。

5. 「search_after 深分页」（正文未写，被问再讲）↔ STAR2。
   口径：实为 from=(pageNo-1)*pageSize 顺序跳过模拟、深页 O(n) 重扫，不要吹"游标语义/O(1)"。

6. 「Maxwell binlog→Kafka→责任链增量同步 OpenSearch 与 ClickHouse」↔ STAR2/讲稿环节⑤。
   口径：增量链路真实；全量重建是 deleteAllIndex 先删的破坏式、无蓝绿兜底，不可吹"可靠重建/蓝绿"；无 ES↔MySQL 每日对账。

7. 「画像查询 681ms→274ms（-59%）、17 组并发 1411ms→357ms」↔ STAR3（真实压测，两数字同源）。
   被追问展开：CompletableFuture 并行聚合 17 组平台数据，线程池 core=10/max=15/queue=100、饱和策略 CallerRunsPolicy 与 DB 连接池 max-active=20 匹配实现背压。如被问基准：是接口压测日志实测，建议面试前确认 p50/环境口径再展开。

8. 「预期 FCP 36.4s→约3s（约12 倍）、首屏 13MB→约4MB、JS 8MB→约1.5MB」↔ STAR4。
   口径：三组数字是素材标注的「结果（预期）」非实测达成，简历已写"预期"，面试继续说"压测预期/Lighthouse 预估"。数字构成：13MB 是全量首屏传输（CSS/JS/图片/其他），JS 8MB 是其中脚本部分，两者是不同测量口径，被问差值如实说明。
   最硬的行为事实：semi-ui 6.4MB（占首屏 49%）经 await import 从首屏 import 链动态摘除（request.ts）。

9. 「三张大图 PNG→JPG 降 87%」（若写或被问）↔ STAR4。
   口径：仅 avatar.jpg 等部分图片落地，登录背景图（1.3MB PNG）未做，只能说"部分图片"。

10. 「400ms 防抖 + React Query 缓存 + IntersectionObserver 懒加载」↔ STAR5。
    口径：防抖 400ms、isResetting 跳过重置无效请求、网络请求量大幅下降。前端筛选器优化。

11. 「端到端链路贯通」↔ 跨 STAR。
    口径：不吹"数量级"——仅 FCP 约12 倍达数量级，后端 681→274（约2.5 倍）、17 并发（约4 倍）、首屏/JS（约3~5 倍）均为数倍提升；全文不写行数/亿级（32.6 亿是雪花直插副作用、真实行数数百~数万未核实）；不作脱敏合规断言（列表接口明文返回联系方式）。

12. 「ClickHouse / 128 维 KNN」（正文未写，聊到再展开）↔ 讲稿环节⑤。
    口径：Maxwell 双路物化落 ClickHouse 供画像明细/报表聚合；向量经火山 Ark 降维至 128 维建 KNN 支撑相似达人召回——可作加分深度。

---

## 五、投递前必填/必改清单

- [ ] 补头部：时间段、角色定位（后端/全栈/前端择一）、**上线状态**（生产 or 联调——素材未明确，务必按真实情况填）
- [ ] 确认压测口径：681/1411 两数字、FCP/首屏/JS 三组数字，面试前定好是"压测日志实测"还是"Lighthouse 预估"
- [ ] 按目标岗位删减：后端岗删职责 4 或压半句；前端岗删职责 1~3 深度、扩写职责 4
- [ ] 全文不含行数/亿级、不含脱敏合规断言、不含"稳定检索"类无支撑形容词
