# mibo 后端三大亮点 · 面试 STAR 稿

> 三个亮点均基于 mibo 实际代码核实（毛利公式、退避策略、CDC 链路等数字可溯源）。
> 每条 = 一句话亮点 + STAR 话术（可直接背）+ 追问 Q&A + 诚实口径。
> ⚠️ 面试前逐条读一遍对应源码，能说出精确数字会非常有说服力。

---

## 亮点一：统一项目毛利计算引擎（StandardProfit）

**一句话**：把原先散落在五套合作模式里的毛利计算合并成一套统一公式，做到底层逻辑一份、口径统一、可审计。

### STAR 话术

> **S（背景）**：达人营销的项目毛利，原来分散在五套合作模式里各自算（纯线上、纯线下对公、纯线下对私、混合模式等），口径不统一，财务对数经常对不上，只能靠人工核对。
>
> **T（任务）**：把五套毛利计算收敛成一套统一公式，让不同合作模式下算出来的毛利口径一致、过程可追溯。
>
> **A（行动）**：
> - 抽象出统一公式：**总毛利 = 客户报价不含税 + 返点不含税 − 线上成本 − 线下成本 − 文化建设费 − 附加税**
> - 难点在**价税分离**。客户报价永远按代理商税率 6% 分离出不含税价：
>   - 线上成本 =（平台报价 + 平台服务费 + 花火费）÷ (1 + 代理商税率)
>   - 线下成本分对公/对私两条分支：
>     - 对公 =（线下合作价 + 赔款）÷ (1 + 达人开票税率)
>     - 对私 =（线下合作价 + 云账户手续费 + 赔款）÷ (1 + 6%)
> - 把"五套模式"收敛成"线上/线下成本走对公还是对私"两种分支，而不是五份独立代码。
> - 用 `@Transactional(REQUIRES_NEW)` 同时支撑**下单自动触发**和**页面手动计算**两个入口。
>
> **R（结果）**：五套逻辑合并成一份，口径统一；下单自动算、页面可手动重算；每一步计算都可追溯。

### 追问 Q&A

- **Q：为什么代理商税率固定 6%？** A：这是财务侧定义的固定值，作为常量/配置存在，可调整；客户报价的价税分离统一用它。
- **Q：为什么用 REQUIRES_NEW？** A：下单主流程事务很大，毛利计算如果跟着主事务一起回滚，已算好的结果也没了；REQUIRES_NEW 挂起外层事务、独立提交，主流程后续失败也不影响毛利结果。
- **Q：手动计算和自动计算有什么差异？** A：自动触发从下单/权益阶段取数；手动计算时 orderId 为空，收款方式、赔款、实收金额为空，公式仍能正常执行，方便补算历史项目。

### 诚实口径

- 公式与分支是真代码（`StandardProfitServiceImpl` 第 54~61 行注释就是这套公式），能背出精确公式是最强的说服点。
- 合作模式枚举（cooperation_mode 0/1/2/3/4 各自含义、线上还是线下）**面试前务必看一遍枚举类确认**，被追问"模式 2 和模式 3 成本怎么拆"要能答。
- 如果你实际没写这部分，面试前至少通读一遍 `unifiedCalculateProjectProfit` 方法全流程。

---

## 亮点二：达人详情采集任务体系（Redis zset 优先级队列）

**一句话**：缺详情的达人自动进入采集队列，按优先级打分排序、失败指数退避、卡死自动兜底恢复。

### STAR 话术

> **S（背景）**：达人库有大量缺详情的达人（只有基础信息，没有作品数据/报价），运营选号时画像不完整、没法评估。原来的拉取没有优先级、重复抓、任务卡死没人管。
>
> **T（任务）**：设计一套自动化采集任务体系——缺详情自动入队、按优先级采集、失败退避重试、卡死兜底恢复。
>
> **A（行动）**：
> - **定时扫描**：cron 每分钟扫描缺详情达人，建任务落库（DB 是任务状态权威）。
> - **优先级打分**：命中 bi_tag 标签 4000 分、命中平台一级分类 3000 分、二级 2000 分、三级 1000 分，同批按 kolId 去重取 max(score)。
> - **Redis zset 就绪队列**：按平台分队列（抖音/快手/B站/小红书各一个 zset key），member 存 `{kolId, taskId, platform}`，score 即优先级，消费端按序取。
> - **卡死判定**：claim_time 超过 5 分钟没回写 → 判卡死转失败。
> - **指数退避**：retry_count 0→1 分钟、1→5 分钟、≥2→30 分钟，最多 3 次，超 3 次置终态失败；状态机用 `WHERE status=FAILED AND next_retry_at IS NULL` 加返回行数计数防并发重入。
> - **孤儿兜底**：待采集超过 10 分钟没人消费的重新入队。
> - 配套**统计大盘接口**（按重试次数分布等）方便排查。
>
> **R（结果）**：缺详情达人有序采集、高优先先采、失败自动退避、卡死自动恢复，链路自闭环。

### 追问 Q&A

- **Q：为什么用 zset 不是普通 List？** A：需要按优先级 score 排序消费，zset 天然支持。
- **Q：DB 和 Redis 谁是权威？** A：DB 是任务状态权威（谁在处理、重试到第几次、终态），Redis zset 只是就绪信令队列，消费端 ZPOP。
- **Q：怎么防重复采集？** A：任务表 (platform, kol_id) 唯一索引，一 kol 一行天然去重。
- **Q：卡死怎么判定、多久？** A：claim_time 超 5 分钟（`STUCK_MINUTES=5`）没回写即判卡死。
- **Q：爬虫挂了会怎样？** A：任务到期重入队 + 孤儿兜底（10 分钟），卡死任务也被 scan job 转失败重新排程，自动恢复。
- **Q：分数从哪来？** A：标签/分类命中加分：bi_tag 4000、一级 3000、二级 2000、三级 1000。

### 诚实口径

- 数字全真实：`STUCK_MINUTES=5`、`MAX_RETRY=3`、退避 1/5/30、`ORPHAN_MINUTES=10`、cron 每分钟。能一口气背出这组数字，面试观感极强。
- ⚠️ **爬虫消费端是既有实现**：zset 是"沿用既有队列 key（爬虫消费方）"。被问"爬虫谁写的"要诚实——任务排程、状态机、退避、卡死兜底是自己实现的，实际抓取由独立爬虫进程消费该 Redis 队列完成。

---

## 亮点三：ClickHouse 实时数仓 + 业务仪表盘

**一句话**：MySQL binlog（Maxwell）→ Kafka → ClickHouse 的 CDC 实时链路，把大屏聚合查询从 MySQL 多表 join 迁到 ClickHouse 列式分析。

### STAR 话术

> **S（背景）**：业务执行概览大屏（管理层看达人漏斗、收入、转化）的数据来自 MySQL 多表聚合，跨表 join 慢、实时性差，埋点/漏斗分析数据量大 MySQL 扛不住。
>
> **T（任务）**：引入 ClickHouse 列式分析库，实时同步业务指标和埋点数据，大屏查询走 ClickHouse。
>
> **A（行动）**：
> - **CDC 链路**：MySQL binlog（Maxwell）→ Kafka → ClickHouse（docker-compose-cdc 部署，Kafka 3.7.1）。
> - **物化视图**：建 `v_stage_reached`（阶段漏斗）、`v_talent_stage_times`（阶段耗时）等视图，大屏聚合查询直接打视图。
> - **指标口径**：9 项业务指标、10 段漏斗、收入趋势/平台分布/埋点分析。
> - **正确性**：`FINAL` 去重 + 软删过滤，保证实时数据准确。
> - **对账兜底**：提供 MySQL↔ClickHouse 行数对账方法，保证两边数据一致。
> - Java 取数层（ClickHouseBusinessOverviewService）做聚合查询，团队均值/转化率等业务换算放上层组装。
>
> **R（结果）**：大屏查询从 MySQL 多表 join 迁移到 ClickHouse 列式分析，查询性能与实时性提升，MySQL 退化为对账兜底。

### 追问 Q&A

- **Q：为什么用 Maxwell + Kafka？** A：方案评估后定的：Maxwell 轻量、把 binlog 转成 JSON 事件，Kafka 做缓冲和分发，消费端可扩展（ClickHouse 落库 + 其他下游）。
- **Q：FINAL 是什么？** A：ClickHouse 的 FINAL 修饰符，对 ReplacingMergeTree 主键相同的重复行做合并去重，保证实时增量数据不重复。
- **Q：实时性怎么保证/有延迟怎么办？** A：链路是近实时（秒级~分钟级延迟），大屏允许近实时；用 MySQL↔ClickHouse 行数对账兜底一致性。
- **Q：大屏 10 段漏斗是哪 10 段？** A：达人推荐→报价→客户待选→询档→权益→权益确认→下单→内容→内容确认→数据监控→结算这条链路上取的阶段（可到代码 `ErpTalentStageLog` / 漏斗配置确认）。

### 诚实口径

- ⚠️ **这条最容易暴露参与深度**。如果 ClickHouse 链路主要是团队/数据同学搭的，面试区分措辞："参与/负责业务概览的 ClickHouse 查询取数层与大屏对接"，别把 CDC 链路全揽到自己头上。
- 链路（Maxwell→Kafka→ClickHouse）、视图名（`mibo_bi.v_stage_reached` / `v_talent_stage_times`）、指标数（9 指标/10 漏斗）都是真实可查的，提前翻一遍 `ClickHouseBusinessOverviewService` 和 `docs/业务仪表盘-*` 文档。

---

## 面试前 checklist

- [ ] 通读 `StandardProfitServiceImpl`（公式 + REQUIRES_NEW + 双入口）
- [ ] 通读 `TalentCollectTaskServiceImpl.scanAndReschedule`（卡死/退避/重入队/孤儿）
- [ ] 通读 `ClickHouseBusinessOverviewService` + docs 里仪表盘文档
- [ ] 确认 cooperation_mode 枚举含义（对公/对私/线上/线下）
- [ ] 确认大屏 10 段漏斗具体阶段
- [ ] 确认自己在三个亮点里的实际参与边界，措辞分别用"负责/参与/对接"
