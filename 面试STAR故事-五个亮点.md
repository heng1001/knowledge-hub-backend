# 达人库面试 STAR 故事（五个亮点）

> 用途：面试前熟读，讲到能自然转述。每个故事按「背景 → 做法 → 结果 → 反思」组织，
> 数字都是你真实代码/日志里的，背熟。
> ⚠️ 每个故事末尾都标了「追问红线」——那是你项目里真实存在的缺口，被追问时主动承认 + 说改进方向。

---

## STAR 1：达人详情采集任务体系（后端，最值得深讲）

**背景**
我们爬虫组是独立团队，达人详情的数据由他们采集。一开始有两大痛点：一是**不知道该采哪些达人**——有些达人详情不齐全，靠人工盯不现实；二是**重复采集和卡死**——同一个达人被反复采，采到一半爬虫挂了就永远卡在"采集中"。

**做法**
我设计了「后端建任务 + Redis zset 调度 + DB 状态机」的 **pull 模型**：
- 定时 Job 汇总 `has_detail=0` 的缺详情达人，按 `(platform, kol_id)` **一 kol 一行**（唯一索引天然去重）建任务；
- 按优先级 score 推入 **Redis zset** 队列（抖音/快手/B站/小红书 4 条）；
- 爬虫 `ZPOPMAX` 领取最高分任务后**直接写 MySQL**，用 CAS 条件更新（`UPDATE ... WHERE status=0/1`）保证一个任务只被处理一次；
- 扫描 Job 做三件事：**5 分钟卡死判定**（超时 CAS 到失败态）、**1/5/30 分钟指数退避**（最多 3 次，超限转终态）、孤儿任务兜底重推。

**结果**
后端完全不感知爬虫并发度，天然去重，故障自愈。"重复采集"和"卡死无人管"两个核心问题都解决了。核心范式是「**Redis 做就绪信令 + DB 做权威状态（CAS）**」——高并发信令走 Redis，最终一致性状态走 DB。

**反思**
踩过一个 MyBatis-Plus 的坑：`updateById` 默认 NOT_NULL 策略**不会把字段更新为 NULL**，清空退避字段必须用 `LambdaUpdateWrapper.set(field, null)`，否则任务会在退避窗口内被反复重入队。这种坑只有真实遇到才会知道。

**追问红线**：终态（失败 4 次）的行会被 enqueue 逻辑重置回"待采集"，如果这个达人永远抓不到，`has_detail` 永远是 0，会陷入循环——这是我已知的设计缺陷，改进方向是终态排除出 enqueue 来源、对永久失败达人打人工标记。

---

## STAR 2：ES 多维达人检索（后端，讲架构）

**背景**
达人主表 `erp_talent` 约 90 列、几乎无二级索引，但运营的核心场景是"抖音 + 美妆 + 粉丝 50w~200w + 互动率 >3% + 报价区间"这种**任意组合筛选**。几十个维度组合，MySQL 单表索引根本无法覆盖。

**做法**
把检索下沉到 ES/OpenSearch：
- **策略模式**抽象出 5 个平台实现（抖音/快手/B站/小红书/视频号），新增平台只加一个策略类；
- 每平台 **3 个索引**（kols 个人信息 / data 商业数据 / price 报价），检索时三索引并行装配；
- 多条件组合：每个筛选条件先查出一个 kolId 集合，再在 Java 侧**有序交集**合并；
- 深分页用 search_after；数据同步用 **Maxwell binlog → Kafka → 责任链**增量 + 火山 DTS 全量重建；
- 做了 **ES/MySQL 三轨切换**，ES 挂了自动降级 MySQL 兜底。

**结果**
支撑了 20+ 维度任意组合筛选；ES 主路径 + MySQL 兜底，同步失败可重放可重建。

**反思**：这套东西的代价是维护约 60 个索引的同步一致性，我关注的改进点是补 ES↔MySQL 每日对账和 binlog 延迟告警；重建流程目前是先删旧的破坏式，理想应该改蓝绿（新索引→校验→切 alias→删旧）。

**追问红线**：search_after 实际是 `from` 顺序跳过的模拟（每跳 1000 条），不是真游标，深页仍是 O(n) 重扫——别把它讲成"游标语义"。

---

## STAR 3：达人画像并发查询优化（后端，数字最硬）

**背景**
达人表单列表页，每展示一个达人要聚合 17 组平台数据（画像/作品/转化能力/报价……）。一开始串行查，最慢一个列表要 1.4 秒。

**做法**
用 `CompletableFuture.supplyAsync + allOf().join()` 做并行化。专门配了一个线程池：core=10、max=15、queue=100、饱和策略 **CallerRunsPolicy**——这个参数不是拍脑袋，是**和数据库连接池 max-active=20 匹配**的，并发打满时靠 CallerRunsPolicy 把多余请求压回调用线程做背压，避免把数据库打死。

**结果**（真实压测日志）
- 画像查询：串行 681ms → 并发 274ms，**提升 59%**（省 407ms）；
- 17 个并发查询：串行总和 1411ms → 并发 357ms。

**反思**
这个案例的亮点不是"我用了并发"，而是**"线程池参数和 DB 连接池匹配 + 背压策略"这个取舍**——面试官追问线程池参数时你能答出"为什么是 10/15/100"，这就是深度。

**追问红线**：无。（这个点你项目做得扎实，可放心讲。）

---

## STAR 4：首屏性能优化（前端，数字最硬）

**背景**
Lighthouse 实测：**FCP 36.4 秒 / LCP 74.7 秒 / 首屏传输 13MB**，其中 `@douyinfe/semi-ui` 一个依赖就 **6.4MB、占 49%**。我定位到一个核心问题：**最贵的依赖挂在最热的 import 链上**——`request.ts` 被所有页面引用，而它同步 import 了整个 Semi UI，导致登录页都背着 6.4MB。

**做法**
顺着"最贵依赖挂最热链路"这条主线砍：
1. `request.ts` 里 Toast/Modal 改成 `await import('@douyinfe/semi-ui')` 动态加载——**收益最大**，6.4MB 从首屏 import 链摘掉；
2. posthog 分析脚本用 `requestIdleCallback` 空闲加载（296KB 不再同步阻塞）；
3. 登录页子组件 `lazy() + Suspense`，用户不点不加载；
4. `semi.css`（778KB）改异步加载 + preload 回退；
5. 三张大图 PNG→JPG，合计降 **87%**。

**结果**（预期）
FCP 36.4s → ~3s（**12 倍**），首屏传输 13MB → ~4MB（**3.2 倍**），首屏 JS 8MB → ~1.5MB。

**反思**：分析框架比数字更值钱——**先看依赖挂在哪条 import 链上**，比盲目分包高效。已知没做完的部分：登录背景图压缩实际未落地（仍是 1.3MB PNG），是下一步。

**追问红线**：图片优化只落地了一部分（`avatar.jpg` 做了，登录 `bg` 没做），被问"图片优化做了吗"要如实说"部分落地，登录页那张还没换"。

---

## STAR 5：达人多维筛选器（前端，讲前后端协同）

**背景**
运营要从达人选号，筛选维度有 20+ 个：达人类型、内容主题、平台、粉丝量档位、CPM、互动率、完播率、报价……条件变化非常频繁。

**做法**
- **400ms 防抖** + `isResetting` ref 跳过重置时的无效请求，避免条件每动一下就打一次接口；
- 一个特殊场景：后端接口不支持"合作中 + 已合作"同时筛选，我在前端**并行发两个请求 + 按 kolId 去重 + mergeCache 缓存**，本地再分页——这是用前端逻辑兜底后端接口能力不足。

**结果**
筛选操作流畅，网络请求量大幅下降；运营能按任意维度组合快速锁下达人。

**反思**：那个"并行拉全量"其实是我已知的**正确性隐患**——每个状态只拉 100 条，命中超过 100 条会静默丢数据。改进方向是把多值筛选下沉到后端 ES（terms/bool 查询），前端只发一页请求。面试时主动说这个，比被戳穿好。

**追问红线**：筛选状态**没有序列化到 URL**（刷新就丢）、列表页**没有虚拟滚动**（每页 10 条后端分页所以还没必要）——别主动讲"可分享筛选条件"或"虚拟滚动"。

---

## 附：怎么选讲哪几个（面试时间不够时的取舍）

- 时间只够讲 **2 个**：STAR 1（pull 采集体系）+ STAR 3（并发优化，数字最硬）
- 时间够讲 **3 个**：再加 STAR 2（ES 检索，体现架构视野）
- 前端岗位/前端多问：STAR 4（性能）+ STAR 5（筛选器）
- 每个故事控制在 60~90 秒，数字必须背准（681→274、1411→357、36.4s→3s、13MB→4MB、6.4MB、87%、1/5/30、5min）
