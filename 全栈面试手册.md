# 全栈面试手册（new-oa + mibo）

> 完整面试准备材料。覆盖：简历数字口径、后端三大亮点 STAR、前端核心亮点 STAR、面试检查清单。
> 独立文件：《后端亮点-面试STAR稿.md》（后端三项详细版）、《全栈开发简历-米播科技.md》（简历正文）。
> 本手册为合并主文档，可单独使用。

---

## 一、简历数字口径总表（面试前背熟）

| 数字 | 出处 | 是否实测 | 面试口径 |
|---|---|---|---|
| FCP 36.4s → 约3s（12x） | PERFORMANCE.md | ❌ 压测预期/Lighthouse 预估 | 说"压测预期"，不说"线上实测达成" |
| LCP 74.7s → 约5s（15x） | PERFORMANCE.md | ❌ 预估 | 同上 |
| 首屏传输 13MB → 约4MB（3.2x） | PERFORMANCE.md | ❌ 预估 | 同上；这是全量传输口径 |
| 首屏 JS 8MB → 约1.5MB（5.3x） | PERFORMANCE.md | ❌ 预估 | 同上；与上一行是不同测量口径 |
| semi-ui.js 6.4MB、占首屏 49% | PERFORMANCE.md | ✅ 打包产物 | **最硬的事实**：request.ts 动态 import 摘除 |
| semi.css 778KB 渲染阻塞 → preload 异步 | PERFORMANCE.md | ✅ 实现落地 | 讲 preload+onload 原理 |
| posthog-js 296KB → requestIdleCallback 延迟 | PERFORMANCE.md | ✅ 实现落地 | 讲空闲加载 + timeout 5s |
| 三张大图 PNG→JPEG 降 87% | PERFORMANCE.md | ✅ 已落地 | 只讲"三张大图"，登录背景图未做 |
| 画像查询 681ms → 274ms（-59%） | 接口压测 | ✅ 压测实测 | 后端画像并发优化，p50/环境口径面试前确认 |
| 17 组并发 1411ms → 357ms | 接口压测 | ✅ 实测 | 线程池 core=10/max=15/queue=100 + CallerRunsPolicy 与 DB max-active=20 匹配 |
| 达人库 20+ 维度组合筛选 | 代码（跨平台累计） | ✅ 真实 | **跨平台累计**维度族，单平台 10~17 维 |
| 采集任务：5min 卡死 / 3次重试 / 1·5·30min 退避 / 10min 孤儿 | 代码 | ✅ 真实 | `STUCK_MINUTES=5` `MAX_RETRY=3` `ORPHAN_MINUTES=10` |
| 打分 4000/3000/2000/1000 | 代码（DouyinKolsOsMapper） | ✅ 真实 | bi_tag→一级→二级→三级 |
| 毛利统一公式 | 代码（StandardProfitServiceImpl） | ✅ 真实 | 见后端亮点一 |

> 一句话铁律：**数字能溯源到代码/文档才说，预估就明说预估；不写行数/亿级/真实数据量。**

---

## 二、后端三大亮点 STAR

### 亮点 1：统一项目毛利计算引擎

**一句话**：把原先散落在五套合作模式里的毛利计算合并成一套统一公式，口径统一、可审计。

- **S**：项目毛利原来分散在五套合作模式（纯线上/纯线下对公/纯线下对私/混合等）里各自算，口径不统一，财务对数对不上。
- **T**：收敛成一套统一公式，任何模式算出来口径一致。
- **A**：
  - 统一公式：**总毛利 = 客户报价不含税 + 返点不含税 − 线上成本 − 线下成本 − 文化建设费 − 附加税**
  - 客户报价永远按代理商税率 6% 价税分离；线上成本 =（平台报价+平台服务费+花火费）÷(1+代理商税率)
  - 线下成本分对公/对私：对公=(线下合作价+赔款)÷(1+达人开票税率)；对私=(线下合作价+云账户手续费+赔款)÷(1+6%)
  - 五套模式收敛成"线上/线下成本走对公还是对私"两种分支；`@Transactional(REQUIRES_NEW)` 支撑下单自动触发 + 页面手动计算双入口
- **R**：五套合并一套，口径统一；下单自动算、页面可重算，过程可追溯。

**追问**：① 6% 为什么固定 → 财务定义常量可调；② REQUIRES_NEW 为什么 → 独立提交，主流程回滚不影响已算结果；③ 手动/自动差异 → 手动时 orderId 空、收款方式/赔款为空，公式仍可算。
**诚实口径**：合作模式枚举（0/1/2/3/4 各自含义）面试前务必看枚举类确认。

### 亮点 2：达人详情采集任务体系

**一句话**：缺详情达人自动入队、按优先级采集、失败指数退避、卡死自动兜底。

- **S**：达人库大量缺详情达人，原拉取无优先级、重复抓、卡死没人管。
- **T**：自动化采集体系——缺详情自动入队、优先级排序、失败重试、卡死恢复。
- **A**：
  - cron 每分钟扫描缺详情达人建任务落库（DB 为状态权威）
  - 优先级打分：bi_tag 4000 / 一级 3000 / 二级 2000 / 三级 1000，同批按 kolId 去重取 max(score)
  - Redis zset 按平台分队列（抖音/快手/B站/小红书），member=`{kolId,taskId,platform}`，score 即优先级
  - 卡死判定：claim_time 超 5 分钟（`STUCK_MINUTES=5`）转失败
  - 退避重试：retry 0→1min、1→5min、≥2→30min，最多 3 次（`MAX_RETRY=3`），超限置终态失败；`WHERE status=FAILED AND next_retry_at IS NULL` 用返回行数防并发重入
  - 孤儿兜底：待采集超 10 分钟（`ORPHAN_MINUTES=10`）重新入队；配套统计大盘接口
- **R**：链路自闭环——有序采集、高优先先采、失败自动退避、卡死自动恢复。

**追问**：① 为什么 zset → 按 score 优先级消费；② DB/Redis 谁权威 → DB 状态权威、zset 是就绪信令；③ 防重复 → (platform,kol_id) 唯一索引一 kol 一行；④ 爬虫挂了 → 到期重入队+孤儿兜底自动恢复；⑤ 卡死多久 → 5 分钟。
**诚实口径**：⚠️ **爬虫消费端是既有实现**，zset 沿用既有队列 key。被问"爬虫谁写的"要诚实——排程/状态机/退避/兜底是自己做的，抓取由独立爬虫进程消费。

### 亮点 3：ClickHouse 实时数仓 + 业务仪表盘

**一句话**：MySQL binlog（Maxwell）→ Kafka → ClickHouse 的 CDC 链路，把大屏聚合从 MySQL 多表 join 迁到 ClickHouse。

- **S**：业务概览大屏数据来自 MySQL 多表聚合，join 慢、实时性差，埋点/漏斗分析 MySQL 扛不住。
- **T**：引入 ClickHouse 列式分析库，实时同步数据，大屏查询走 ClickHouse。
- **A**：
  - CDC 链路：MySQL binlog（Maxwell）→ Kafka → ClickHouse（docker-compose-cdc，Kafka 3.7.1）
  - 物化视图：`v_stage_reached`（阶段漏斗）、`v_talent_stage_times`（阶段耗时）等，聚合直接打视图
  - 指标：9 项业务指标、10 段漏斗、收入趋势/平台分布/埋点分析
  - 正确性：`FINAL` 去重 + 软删过滤；提供 MySQL↔ClickHouse 行数对账
  - Java 取数层（ClickHouseBusinessOverviewService）聚合查询，团队均值/转化率放上层
- **R**：大屏查询迁移到列式分析，性能与实时性提升，MySQL 退化为对账兜底。

**追问**：① 为什么 Maxwell+Kafka → 轻量、binlog 转 JSON、Kafka 缓冲分发；② FINAL → ReplacingMergeTree 主键合并去重；③ 实时性 → 近实时，对账兜底；④ 10 段漏斗是哪 10 段 → 推荐→报价→…→结算链路（提前到代码确认）。
**诚实口径**：⚠️ 这条最暴露参与深度。若链路是团队搭的，措辞用"负责/参与业务概览的 ClickHouse 查询取数层与大屏对接"，别全揽。

---

## 三、前端核心亮点 STAR

### 亮点 1：后端菜单驱动的动态路由 + RBAC 按钮级权限

**一句话**：前端根据后端菜单动态生成路由，后端加菜单/权限前端零改动，按钮级权限可控。

- **S**：从 Vue3（芋道 ruoyi-vue-pro）迁移到 React，后端菜单还是 Vue 格式（component 带 `.vue` 后缀），要无缝继承后端 RBAC 权限体系。
- **T**：后端菜单 → 动态生成 React 路由，同时做按钮级权限控制。
- **A**：
  - `collectRoutes` 递归构建路由树：`stripVueSuffix` 抹掉 `.vue` 后缀、`resolveElement` 补 `/index`、目录类型菜单（component 空/Layout/ParentView）不生成路由只递归
  - 登录流程：AuthGuard 校验登录 → `fetchPermissionInfo` 拉权限 → `buildDynamicRoutes` → 重建 `createBrowserRouter`
  - 按钮级权限：`useAuthStore` 持有 permissions，页面 `permissions.includes(EXPORT_PERMISSION)` 控制导出等按钮显隐
  - 多租户：请求头统一带 `tenant-id`
- **R**：后端加菜单/权限前端无需发版；静态详情页 + 404 兜底挂 AuthGuard 内，刷新不 404。

**追问**：① 刷新页面 404 怎么处理 → staticRoutes 挂 AuthGuard 内；② 为什么菜单带 .vue → 后端芋道 Vue 遗留配置，前端抹后缀兼容；③ 权限在哪拉 → 登录后 fetchPermissionInfo 一次。

### 亮点 2：axios 请求封装 + Token 刷新队列

**一句话**：并发 401 只刷新一次 token，其余请求排队刷新完成后自动重放，无重复刷新、无请求丢失。

- **S**：页面多个并发请求同时 401，若各自刷新会重复刷新、甚至相互覆盖 token。
- **T**：并发 401 单飞刷新 + 队列重放。
- **A**：
  - 单飞互斥：刷新中不再发起新刷新
  - 请求队列：刷新中的 401 请求 `requestQueue.push(...)` 排队；刷新成功后 `requestQueue.forEach(cb => cb())` 统一重放并清空
  - 刷新失败：清空队列，跳登录
  - 配套：白名单接口不携带 token、GET 防缓存、Blob 下载识别、业务错误码统一 Toast、RSA 登录加密、code 901 演示模式拦截
- **R**：并发 401 只刷一次、全量重放，稳定不丢请求。

**追问**：① 白名单怎么处理 → 不带 token 不拦截；② 队列会泄漏吗 → 成功/失败都会清空；③ 刷新 token 请求本身 401 怎么办 → 清空队列跳登录。

### 亮点 3：登录首屏性能专项（FCP 36.4s → 约3s）

**一句话**：把登录页从"几乎不可用"优化到秒级，靠的是砍掉首屏阻塞资源。

- **S**：Lighthouse FCP 36.4s、LCP 74.7s。瓶颈：semi.css 778KB 渲染阻塞、request.ts 静态 import Semi UI 拉入 6.4MB（占首屏 49%）、posthog 296KB 同步初始化、背景图 1.5MB。
- **T**：首屏提速到秒级。
- **A**：
  - **semi.css 778KB render-blocking → `preload + onload` 异步化**（noscript 降级），不阻塞首屏绘制
  - **request.ts 里 Modal/Toast 改动态 import，把 6.4MB Semi UI 从首屏 import 链摘除（收益最大）**
  - posthog-js 296KB → `requestIdleCallback` + 动态 import 延迟加载（timeout 5s 兜底）
  - 登录页子组件（忘记密码/免费试用）`React.lazy`
  - 三张大图 PNG→JPEG 降 87%；Vite `manualChunks` 手动分包（semi-ui/react-vendor/query/zustand/axios/vendors）
- **R**（预估）：FCP 36.4s→约3s（12x）、LCP 74.7s→约5s（15x）、首屏传输 13MB→约4MB、首屏 JS 8MB→约1.5MB。

**追问**：① preload 原理 → `<link rel=preload as=style onload>` 切换 rel=stylesheet，无 JS 降级；② requestIdleCallback 是什么 → 浏览器空闲回调，配合动态 import 拆 chunk；③ 为什么动 Semi 收益最大 → 6.4MB 占首屏 49%，摘除即砍掉近半。
**诚实口径**：四组数字是**压测预期/Lighthouse 预估**非线上实测；图片只落地"三张大图"，登录背景图未做。

### 亮点 4：Vue3 → React 全量迁移（可作开场）

**一句话**：把公司原 Vue3 后台全量重写为 React 19 + TS 单页应用，约 23 万行、700+ 组件、20+ 业务模块。

- **S**：公司 OA 原为 Vue3（芋道），前端团队迁移 React 19。
- **A**：全量重写 React 19 + TS + Vite + Semi Design；系统管理/CRM/ERP 选号项目/BPM 审批/财务/达人库/报表等 20+ 模块；后端菜单驱动路由无缝衔接；`ColumnConfig/TableColumnConfig` 动态表头、`StageFlowPanel` 阶段流程、`SensitiveField` 脱敏等组件沉淀复用。
- **R**：新栈落地、老功能全量覆盖，业务组件沉淀成库。
**追问**：迁移怎么保证功能一致 → 按模块分批 + 后端菜单驱动 + 组件复用；你负责哪些模块 → 提前列好自己主导的模块清单。

### 其余前端点 Q&A 速查

- **ECharts 业务大屏**：BaseChart 封装，`IntersectionObserver` 进入视口才创建 ECharts 实例、`ResizeObserver + rAF` 防抖 resize、`lazyUpdate` 增量 setOption 不重建实例；14 个后端接口实时数据、10 段漏斗/收入趋势/排名环图/风险预警。
- **企业微信集成**：`useWecomEnterpriseChat` 封装 register/openEnterpriseChat/分享，注册去重、Promise 化 + 超时（10s/30s）、URL 去 hash；UA 环境检测，非企微降级提示；点头像拉起会话/查看资料。
- **敏感字段脱敏审计**：`SensitiveField` 组件，查看手机号/银行账号必须填原因+备注，后端记审计日志，Context 级批量查看/隐藏。
- **版本热更新检测**：构建生成 `version.json`（含 commit hash），前端定时 + tab 聚焦轮询，发现新版本强制刷新；`ChunkErrorBoundary` 兜底 chunk 404。
- **全局字典**：登录时一次拉取全部字典全局缓存，页面零请求；`DictTag` 统一渲染，禁止硬编码枚举。

---

## 四、面试检查清单

**数字口径**
- [ ] 四组性能数字（FCP/LCP/首屏/JS）统一说"压测预期/Lighthouse 预估"
- [ ] "20+ 维度"说清是跨平台累计，单平台 10~17 维
- [ ] 681/1411 两个压测数字确认 p50/环境口径

**后端**
- [ ] 通读 `StandardProfitServiceImpl`（公式 + REQUIRES_NEW + 双入口）
- [ ] 通读 `TalentCollectTaskServiceImpl.scanAndReschedule`（卡死/退避/重入队/孤儿）
- [ ] 通读 `ClickHouseBusinessOverviewService` + docs 仪表盘文档
- [ ] 确认 cooperation_mode 枚举含义、大屏 10 段漏斗具体阶段
- [ ] 明确自己在三项里的参与边界，措辞用"负责/参与/对接"

**前端**
- [ ] 背熟 `request.ts` 刷新队列流程（isRefreshing 互斥 + requestQueue 重放）
- [ ] 背熟动态路由兼容处理（stripVueSuffix / 补 index / Layout 递归）
- [ ] 背熟性能优化 4 个动作 + 各自原理（preload / 动态 import / requestIdleCallback / 分包）
- [ ] 准备 2~3 个自己主导的前端模块，能展开讲实现细节

**行为**
- [ ] 开场用「Vue3→React 全量迁移」控场
- [ ] 被追问超出主导范围的功能，先诚实说明边界再讲机制
